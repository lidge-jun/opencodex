// @ts-check
export {};

/** @type {Record<string, Record<string, string>>} */
const translations = {
  en: {
    tagline: "One guarded passage to your AI models", headlineA: "Access the", headlineB: "model rail.", description: "Sign in, add credit, create an API key, and follow every balance movement. Provider credentials stay behind the private routing core.", client: "Your client", access: "hubapi access", providers: "Private model routes", welcome: "Welcome to hubapi", signIn: "Sign in", register: "Create account", email: "Email", password: "Password", passwordHint: "Use at least 12 characters.", signInButton: "Sign in securely", createAccount: "Create account", dashboard: "Overview", keys: "API keys", recharge: "Recharge", admin: "Admin", logout: "Sign out", balance: "Credit balance", available: "Available", reserved: "Reserved", pricing: "Completed request price", live: "Passage online", passageClient: "Client authenticated", passageAdmission: "Credit admitted", passageRoute: "Private model route", myKeys: "Your API keys", createKey: "Create key", keyName: "Key name", noKeys: "No API keys yet. Create one for your first client.", revoke: "Revoke", ledger: "Credit ledger", noLedger: "No balance movement yet.", redeemTitle: "Redeem recharge code", code: "Recharge code", redeem: "Add credit", revealed: "Copy this key now", saveNow: "It cannot be shown again after you leave this message.", batchTitle: "Create recharge batch", label: "Batch label", units: "Units per code", quantity: "Number of codes", generate: "Generate codes", generated: "Generated once-only codes", refresh: "Refresh", genericError: "The action was not completed. Check the fields and try again.", sessionExpired: "Your session ended. Sign in again.", creditUnit: "units", roleAdmin: "Administrator", roleUser: "User", fixedPricing: "Fixed per completed request · version request-v1"
  },
  "zh-CN": {
    tagline: "一条受控通道，连接全部 AI 模型", headlineA: "进入你的", headlineB: "模型通行轨。", description: "登录、充值、创建 API 密钥，并核对每一笔额度变化。提供方凭据始终留在私有路由核心后方。", client: "你的客户端", access: "hubapi 准入", providers: "私有模型路由", welcome: "欢迎使用 hubapi", signIn: "登录", register: "创建账户", email: "邮箱", password: "密码", passwordHint: "至少使用 12 个字符。", signInButton: "安全登录", createAccount: "创建账户", dashboard: "总览", keys: "API 密钥", recharge: "卡密充值", admin: "管理", logout: "退出登录", balance: "额度余额", available: "可用", reserved: "已预留", pricing: "已完成请求单价", live: "通道在线", passageClient: "客户端已认证", passageAdmission: "额度已准入", passageRoute: "私有模型路由", myKeys: "你的 API 密钥", createKey: "创建密钥", keyName: "密钥名称", noKeys: "还没有 API 密钥，为第一个客户端创建一个。", revoke: "撤销", ledger: "额度账本", noLedger: "暂无额度变化。", redeemTitle: "兑换充值卡密", code: "充值卡密", redeem: "充值额度", revealed: "现在复制这枚密钥", saveNow: "离开本提示后将无法再次查看。", batchTitle: "创建卡密批次", label: "批次名称", units: "每枚卡密额度", quantity: "卡密数量", generate: "生成卡密", generated: "仅本次显示的卡密", refresh: "刷新", genericError: "操作未完成，请检查输入后重试。", sessionExpired: "会话已结束，请重新登录。", creditUnit: "额度", roleAdmin: "管理员", roleUser: "用户", fixedPricing: "每个完成请求固定扣费 · 版本 request-v1"
  },
  "zh-TW": {
    tagline: "一條受控通道，連接全部 AI 模型", headlineA: "進入你的", headlineB: "模型通行軌。", description: "登入、儲值、建立 API 金鑰，並核對每一筆額度變化。提供方憑證始終留在私有路由核心後方。", client: "你的客戶端", access: "hubapi 准入", providers: "私有模型路由", welcome: "歡迎使用 hubapi", signIn: "登入", register: "建立帳戶", email: "電子郵件", password: "密碼", passwordHint: "至少使用 12 個字元。", signInButton: "安全登入", createAccount: "建立帳戶", dashboard: "總覽", keys: "API 金鑰", recharge: "卡密儲值", admin: "管理", logout: "登出", balance: "額度餘額", available: "可用", reserved: "已預留", pricing: "已完成請求單價", live: "通道上線", passageClient: "客戶端已認證", passageAdmission: "額度已准入", passageRoute: "私有模型路由", myKeys: "你的 API 金鑰", createKey: "建立金鑰", keyName: "金鑰名稱", noKeys: "尚無 API 金鑰，請為第一個客戶端建立一枚。", revoke: "撤銷", ledger: "額度帳本", noLedger: "尚無額度變化。", redeemTitle: "兌換儲值卡密", code: "儲值卡密", redeem: "加入額度", revealed: "立即複製此金鑰", saveNow: "離開此訊息後將無法再次查看。", batchTitle: "建立卡密批次", label: "批次名稱", units: "每枚卡密額度", quantity: "卡密數量", generate: "產生卡密", generated: "僅本次顯示的卡密", refresh: "重新整理", genericError: "操作未完成，請檢查欄位後重試。", sessionExpired: "工作階段已結束，請重新登入。", creditUnit: "額度", roleAdmin: "管理員", roleUser: "使用者", fixedPricing: "每個完成請求固定扣費 · 版本 request-v1"
  },
  de: {
    tagline: "Ein geschützter Zugang zu Ihren KI-Modellen", headlineA: "Betreten Sie die", headlineB: "Modellstrecke.", description: "Anmelden, Guthaben laden, API-Schlüssel erstellen und jede Buchung prüfen. Anbieterzugänge bleiben hinter der privaten Routing-Kern.", client: "Ihr Client", access: "hubapi-Zugang", providers: "Private Modellrouten", welcome: "Willkommen bei hubapi", signIn: "Anmelden", register: "Konto erstellen", email: "E-Mail", password: "Passwort", passwordHint: "Mindestens 12 Zeichen verwenden.", signInButton: "Sicher anmelden", createAccount: "Konto erstellen", dashboard: "Übersicht", keys: "API-Schlüssel", recharge: "Guthaben", admin: "Verwaltung", logout: "Abmelden", balance: "Guthabenstand", available: "Verfügbar", reserved: "Reserviert", pricing: "Preis je abgeschlossener Anfrage", live: "Zugang online", passageClient: "Client authentifiziert", passageAdmission: "Guthaben freigegeben", passageRoute: "Private Modellroute", myKeys: "Ihre API-Schlüssel", createKey: "Schlüssel erstellen", keyName: "Schlüsselname", noKeys: "Noch keine API-Schlüssel.", revoke: "Widerrufen", ledger: "Guthabenjournal", noLedger: "Noch keine Buchungen.", redeemTitle: "Aufladecode einlösen", code: "Aufladecode", redeem: "Guthaben hinzufügen", revealed: "Diesen Schlüssel jetzt kopieren", saveNow: "Er wird danach nicht erneut angezeigt.", batchTitle: "Code-Stapel erstellen", label: "Stapelname", units: "Einheiten pro Code", quantity: "Anzahl", generate: "Codes erzeugen", generated: "Einmalig angezeigte Codes", refresh: "Aktualisieren", genericError: "Aktion nicht abgeschlossen. Eingaben prüfen und erneut versuchen.", sessionExpired: "Sitzung beendet. Bitte erneut anmelden.", creditUnit: "Einheiten", roleAdmin: "Administrator", roleUser: "Benutzer", fixedPricing: "Festpreis pro abgeschlossener Anfrage · Version request-v1"
  },
  fr: {
    tagline: "Un passage protégé vers vos modèles IA", headlineA: "Accédez à la", headlineB: "voie des modèles.", description: "Connectez-vous, rechargez, créez une clé API et suivez chaque mouvement. Les identifiants fournisseur restent derrière le cœur de routage privé.", client: "Votre client", access: "Accès hubapi", providers: "Routes privées", welcome: "Bienvenue sur hubapi", signIn: "Connexion", register: "Créer un compte", email: "E-mail", password: "Mot de passe", passwordHint: "Utilisez au moins 12 caractères.", signInButton: "Se connecter", createAccount: "Créer le compte", dashboard: "Aperçu", keys: "Clés API", recharge: "Recharger", admin: "Admin", logout: "Déconnexion", balance: "Solde de crédits", available: "Disponible", reserved: "Réservé", pricing: "Prix par requête terminée", live: "Passage en ligne", passageClient: "Client authentifié", passageAdmission: "Crédit admis", passageRoute: "Route modèle privée", myKeys: "Vos clés API", createKey: "Créer une clé", keyName: "Nom de la clé", noKeys: "Aucune clé API pour le moment.", revoke: "Révoquer", ledger: "Journal des crédits", noLedger: "Aucun mouvement pour le moment.", redeemTitle: "Utiliser un code", code: "Code de recharge", redeem: "Ajouter le crédit", revealed: "Copiez cette clé maintenant", saveNow: "Elle ne sera plus affichée ensuite.", batchTitle: "Créer un lot de codes", label: "Nom du lot", units: "Unités par code", quantity: "Quantité", generate: "Générer", generated: "Codes affichés une seule fois", refresh: "Actualiser", genericError: "Action non terminée. Vérifiez les champs puis réessayez.", sessionExpired: "Session terminée. Reconnectez-vous.", creditUnit: "unités", roleAdmin: "Administrateur", roleUser: "Utilisateur", fixedPricing: "Prix fixe par requête terminée · version request-v1"
  },
  ja: {
    tagline: "AIモデルへ続く、保護された一本の経路", headlineA: "モデルレールへ", headlineB: "アクセス。", description: "ログイン、残高追加、APIキー作成、すべての残高変動を確認できます。プロバイダー認証情報は非公開ルーティングコア内に留まります。", client: "クライアント", access: "hubapi認証", providers: "非公開モデルルート", welcome: "hubapiへようこそ", signIn: "ログイン", register: "アカウント作成", email: "メール", password: "パスワード", passwordHint: "12文字以上を使用してください。", signInButton: "安全にログイン", createAccount: "アカウント作成", dashboard: "概要", keys: "APIキー", recharge: "チャージ", admin: "管理", logout: "ログアウト", balance: "クレジット残高", available: "利用可能", reserved: "予約済み", pricing: "完了リクエスト単価", live: "経路オンライン", passageClient: "クライアント認証済み", passageAdmission: "クレジット承認済み", passageRoute: "非公開モデルルート", myKeys: "APIキー", createKey: "キーを作成", keyName: "キー名", noKeys: "APIキーはまだありません。", revoke: "失効", ledger: "クレジット台帳", noLedger: "残高変動はまだありません。", redeemTitle: "チャージコードを利用", code: "チャージコード", redeem: "残高を追加", revealed: "今すぐキーをコピー", saveNow: "この表示を離れると再表示できません。", batchTitle: "コードバッチ作成", label: "バッチ名", units: "コードごとの単位", quantity: "数量", generate: "コード生成", generated: "一度だけ表示されるコード", refresh: "更新", genericError: "操作を完了できませんでした。入力を確認してください。", sessionExpired: "セッションが終了しました。再度ログインしてください。", creditUnit: "単位", roleAdmin: "管理者", roleUser: "ユーザー", fixedPricing: "完了リクエストごとの固定料金 · request-v1"
  },
  ko: {
    tagline: "AI 모델로 향하는 하나의 보호된 통로", headlineA: "모델 레일에", headlineB: "접속하세요.", description: "로그인하고 크레딧을 충전하며 API 키와 모든 잔액 변동을 관리하세요. 공급자 자격 증명은 비공개 라우팅 코어 뒤에 유지됩니다.", client: "클라이언트", access: "hubapi 승인", providers: "비공개 모델 경로", welcome: "hubapi에 오신 것을 환영합니다", signIn: "로그인", register: "계정 만들기", email: "이메일", password: "비밀번호", passwordHint: "12자 이상을 사용하세요.", signInButton: "안전하게 로그인", createAccount: "계정 만들기", dashboard: "개요", keys: "API 키", recharge: "충전", admin: "관리", logout: "로그아웃", balance: "크레딧 잔액", available: "사용 가능", reserved: "예약됨", pricing: "완료 요청 단가", live: "통로 온라인", passageClient: "클라이언트 인증됨", passageAdmission: "크레딧 승인됨", passageRoute: "비공개 모델 경로", myKeys: "API 키", createKey: "키 만들기", keyName: "키 이름", noKeys: "아직 API 키가 없습니다.", revoke: "폐기", ledger: "크레딧 원장", noLedger: "아직 잔액 변동이 없습니다.", redeemTitle: "충전 코드 사용", code: "충전 코드", redeem: "크레딧 추가", revealed: "지금 이 키를 복사하세요", saveNow: "이 메시지를 닫으면 다시 볼 수 없습니다.", batchTitle: "충전 코드 묶음 만들기", label: "묶음 이름", units: "코드당 단위", quantity: "수량", generate: "코드 생성", generated: "한 번만 표시되는 코드", refresh: "새로고침", genericError: "작업을 완료하지 못했습니다. 입력을 확인하세요.", sessionExpired: "세션이 종료되었습니다. 다시 로그인하세요.", creditUnit: "단위", roleAdmin: "관리자", roleUser: "사용자", fixedPricing: "완료 요청당 고정 요금 · request-v1"
  },
  ru: {
    tagline: "Один защищённый путь к вашим ИИ-моделям", headlineA: "Войдите на", headlineB: "маршрут моделей.", description: "Войдите, пополните баланс, создайте API-ключ и отслеживайте каждое движение. Данные провайдеров остаются за приватным ядром маршрутизации.", client: "Ваш клиент", access: "Доступ hubapi", providers: "Приватные маршруты", welcome: "Добро пожаловать в hubapi", signIn: "Войти", register: "Создать аккаунт", email: "Эл. почта", password: "Пароль", passwordHint: "Используйте не менее 12 символов.", signInButton: "Безопасный вход", createAccount: "Создать аккаунт", dashboard: "Обзор", keys: "API-ключи", recharge: "Пополнение", admin: "Админ", logout: "Выйти", balance: "Баланс кредитов", available: "Доступно", reserved: "Зарезервировано", pricing: "Цена завершённого запроса", live: "Канал работает", passageClient: "Клиент проверен", passageAdmission: "Кредит разрешён", passageRoute: "Приватный маршрут модели", myKeys: "Ваши API-ключи", createKey: "Создать ключ", keyName: "Название ключа", noKeys: "API-ключей пока нет.", revoke: "Отозвать", ledger: "Журнал кредитов", noLedger: "Движений пока нет.", redeemTitle: "Активировать код", code: "Код пополнения", redeem: "Добавить кредит", revealed: "Скопируйте ключ сейчас", saveNow: "После закрытия он больше не показывается.", batchTitle: "Создать пакет кодов", label: "Название пакета", units: "Единиц на код", quantity: "Количество", generate: "Создать коды", generated: "Коды показываются один раз", refresh: "Обновить", genericError: "Действие не выполнено. Проверьте поля и повторите.", sessionExpired: "Сеанс завершён. Войдите снова.", creditUnit: "ед.", roleAdmin: "Администратор", roleUser: "Пользователь", fixedPricing: "Фиксированно за завершённый запрос · request-v1"
  },
  tr: {
    tagline: "Yapay zekâ modellerinize korumalı tek geçiş", headlineA: "Model hattına", headlineB: "erişin.", description: "Oturum açın, kredi ekleyin, API anahtarı oluşturun ve her bakiye hareketini izleyin. Sağlayıcı bilgileri özel yönlendirme çekirdeğinin arkasında kalır.", client: "İstemciniz", access: "hubapi erişimi", providers: "Özel model rotaları", welcome: "hubapi'ye hoş geldiniz", signIn: "Oturum aç", register: "Hesap oluştur", email: "E-posta", password: "Parola", passwordHint: "En az 12 karakter kullanın.", signInButton: "Güvenli oturum aç", createAccount: "Hesap oluştur", dashboard: "Genel bakış", keys: "API anahtarları", recharge: "Kredi yükle", admin: "Yönetim", logout: "Çıkış yap", balance: "Kredi bakiyesi", available: "Kullanılabilir", reserved: "Ayrılmış", pricing: "Tamamlanan istek fiyatı", live: "Geçiş çevrimiçi", passageClient: "İstemci doğrulandı", passageAdmission: "Kredi kabul edildi", passageRoute: "Özel model rotası", myKeys: "API anahtarlarınız", createKey: "Anahtar oluştur", keyName: "Anahtar adı", noKeys: "Henüz API anahtarı yok.", revoke: "İptal et", ledger: "Kredi defteri", noLedger: "Henüz bakiye hareketi yok.", redeemTitle: "Yükleme kodu kullan", code: "Yükleme kodu", redeem: "Kredi ekle", revealed: "Bu anahtarı şimdi kopyalayın", saveNow: "Bu iletiden ayrılınca yeniden gösterilmez.", batchTitle: "Kod grubu oluştur", label: "Grup adı", units: "Kod başına birim", quantity: "Adet", generate: "Kod oluştur", generated: "Yalnızca bir kez gösterilen kodlar", refresh: "Yenile", genericError: "İşlem tamamlanmadı. Alanları kontrol edip yeniden deneyin.", sessionExpired: "Oturum sona erdi. Yeniden giriş yapın.", creditUnit: "birim", roleAdmin: "Yönetici", roleUser: "Kullanıcı", fixedPricing: "Tamamlanan istek başına sabit ücret · request-v1"
  }
};

// Keep admin-only labels separate from the shared portal dictionary so each
// supported locale can be checked as one complete, explicit set.
const interfaceTranslations = {
  en: { proxy: "Proxy", proxyDescription: "Use one public key at the allowlisted edge. Provider credentials and the management API stay private.", edgeReady: "Edge ready", upstreamNotProbed: "Private upstream not probed", publicEndpoints: "Public endpoints", adminDashboard: "Admin dashboard", usersTotal: "Users", usersActive: "Active users", creditIssued: "Issued credit", batchesActive: "Active batches", proxySafety: "Proxy safety", userReference: "User reference", status: "Status", auditTrail: "Audit trail", createBatch: "Create batch", importBatch: "Import codes", adjustCredit: "Adjust credit", reason: "Reason", language: "Language", accountConsole: "Account console", quickSetup: "Quick setup", baseUrl: "Proxy Base URL", copy: "Copy", copied: "Copied", useKeyPlaceholder: "Replace the placeholder with a key created on the API keys page.", batches: "Recharge batches", codeInventory: "Code inventory", noRecords: "No records yet." },
  "zh-CN": { proxy: "代理", proxyDescription: "公开密钥只进入白名单代理边缘；提供方凭据与管理 API 始终留在私有侧。", edgeReady: "边缘已就绪", upstreamNotProbed: "私有上游未探测", publicEndpoints: "公开端点", adminDashboard: "管理员仪表盘", usersTotal: "用户总数", usersActive: "活跃用户", creditIssued: "已发放额度", batchesActive: "活跃批次", proxySafety: "代理安全", userReference: "用户代号", status: "状态", auditTrail: "审计记录", createBatch: "创建批次", importBatch: "导入卡密", adjustCredit: "调整额度", reason: "原因", language: "语言", accountConsole: "账户控制台", quickSetup: "快速接入", baseUrl: "代理 Base URL", copy: "复制", copied: "已复制", useKeyPlaceholder: "请用 API 密钥页面创建的密钥替换示例占位符。", batches: "卡密批次", codeInventory: "卡密库存", noRecords: "暂无记录。" },
  "zh-TW": { proxy: "代理", proxyDescription: "公開金鑰只進入白名單代理邊緣；提供方憑證與管理 API 始終留在私有側。", edgeReady: "邊緣已就緒", upstreamNotProbed: "私有上游未探測", publicEndpoints: "公開端點", adminDashboard: "管理員儀表板", usersTotal: "使用者總數", usersActive: "活躍使用者", creditIssued: "已發放額度", batchesActive: "活躍批次", proxySafety: "代理安全", userReference: "使用者代號", status: "狀態", auditTrail: "稽核記錄", createBatch: "建立批次", importBatch: "匯入卡密", adjustCredit: "調整額度", reason: "原因", language: "語言", accountConsole: "帳戶控制台", quickSetup: "快速接入", baseUrl: "代理 Base URL", copy: "複製", copied: "已複製", useKeyPlaceholder: "請用 API 金鑰頁面建立的金鑰取代範例佔位符。", batches: "卡密批次", codeInventory: "卡密庫存", noRecords: "暫無記錄。" },
  de: { proxy: "Proxy", proxyDescription: "Öffentliche Schlüssel erreichen nur den freigegebenen Edge. Anbieterzugänge und Management-API bleiben privat.", edgeReady: "Edge bereit", upstreamNotProbed: "Privater Upstream nicht geprüft", publicEndpoints: "Öffentliche Endpunkte", adminDashboard: "Admin-Dashboard", usersTotal: "Benutzer", usersActive: "Aktive Benutzer", creditIssued: "Ausgegebenes Guthaben", batchesActive: "Aktive Stapel", proxySafety: "Proxy-Sicherheit", userReference: "Benutzerreferenz", status: "Status", auditTrail: "Audit-Protokoll", createBatch: "Stapel erstellen", importBatch: "Codes importieren", adjustCredit: "Guthaben anpassen", reason: "Grund", language: "Sprache", accountConsole: "Kontokonsole", quickSetup: "Schnelleinrichtung", baseUrl: "Proxy-Basis-URL", copy: "Kopieren", copied: "Kopiert", useKeyPlaceholder: "Ersetzen Sie den Platzhalter durch einen Schlüssel von der API-Schlüsselseite.", batches: "Aufladecode-Stapel", codeInventory: "Code-Bestand", noRecords: "Noch keine Einträge." },
  fr: { proxy: "Proxy", proxyDescription: "La clé publique atteint uniquement la passerelle autorisée. Les accès fournisseur et l’API de gestion restent privés.", edgeReady: "Passerelle prête", upstreamNotProbed: "Amont privé non sondé", publicEndpoints: "Points d’accès publics", adminDashboard: "Tableau d’administration", usersTotal: "Utilisateurs", usersActive: "Utilisateurs actifs", creditIssued: "Crédit émis", batchesActive: "Lots actifs", proxySafety: "Sécurité du proxy", userReference: "Référence utilisateur", status: "État", auditTrail: "Journal d’audit", createBatch: "Créer un lot", importBatch: "Importer des codes", adjustCredit: "Ajuster le crédit", reason: "Motif", language: "Langue", accountConsole: "Console du compte", quickSetup: "Configuration rapide", baseUrl: "URL de base du proxy", copy: "Copier", copied: "Copié", useKeyPlaceholder: "Remplacez le texte d’exemple par une clé créée dans la page des clés API.", batches: "Lots de recharge", codeInventory: "Inventaire des codes", noRecords: "Aucun enregistrement." },
  ja: { proxy: "プロキシ", proxyDescription: "公開キーは許可済みエッジだけに届きます。プロバイダー認証情報と管理APIは非公開です。", edgeReady: "エッジ準備完了", upstreamNotProbed: "非公開上流は未確認", publicEndpoints: "公開エンドポイント", adminDashboard: "管理ダッシュボード", usersTotal: "ユーザー", usersActive: "有効ユーザー", creditIssued: "発行済みクレジット", batchesActive: "有効バッチ", proxySafety: "プロキシ安全性", userReference: "ユーザー参照", status: "状態", auditTrail: "監査ログ", createBatch: "バッチ作成", importBatch: "コード取込", adjustCredit: "クレジット調整", reason: "理由", language: "言語", accountConsole: "アカウントコンソール", quickSetup: "クイック設定", baseUrl: "プロキシ Base URL", copy: "コピー", copied: "コピー済み", useKeyPlaceholder: "APIキーページで作成したキーにプレースホルダーを置き換えてください。", batches: "チャージコードバッチ", codeInventory: "コード在庫", noRecords: "記録はまだありません。" },
  ko: { proxy: "프록시", proxyDescription: "공개 키는 허용된 엣지만 통과합니다. 공급자 자격 증명과 관리 API는 비공개로 유지됩니다.", edgeReady: "엣지 준비됨", upstreamNotProbed: "비공개 업스트림 미확인", publicEndpoints: "공개 엔드포인트", adminDashboard: "관리자 대시보드", usersTotal: "사용자", usersActive: "활성 사용자", creditIssued: "발급 크레딧", batchesActive: "활성 묶음", proxySafety: "프록시 안전", userReference: "사용자 참조", status: "상태", auditTrail: "감사 기록", createBatch: "묶음 만들기", importBatch: "코드 가져오기", adjustCredit: "크레딧 조정", reason: "사유", language: "언어", accountConsole: "계정 콘솔", quickSetup: "빠른 설정", baseUrl: "프록시 Base URL", copy: "복사", copied: "복사됨", useKeyPlaceholder: "API 키 페이지에서 만든 키로 자리표시자를 바꾸세요.", batches: "충전 코드 묶음", codeInventory: "코드 재고", noRecords: "아직 기록이 없습니다." },
  ru: { proxy: "Прокси", proxyDescription: "Публичный ключ поступает только на разрешённый шлюз. Доступы провайдеров и API управления остаются приватными.", edgeReady: "Шлюз готов", upstreamNotProbed: "Приватный upstream не проверен", publicEndpoints: "Публичные эндпоинты", adminDashboard: "Панель администратора", usersTotal: "Пользователи", usersActive: "Активные пользователи", creditIssued: "Выданный кредит", batchesActive: "Активные пакеты", proxySafety: "Безопасность прокси", userReference: "Ссылка на пользователя", status: "Статус", auditTrail: "Журнал аудита", createBatch: "Создать пакет", importBatch: "Импорт кодов", adjustCredit: "Изменить кредит", reason: "Причина", language: "Язык", accountConsole: "Консоль аккаунта", quickSetup: "Быстрая настройка", baseUrl: "Базовый URL прокси", copy: "Копировать", copied: "Скопировано", useKeyPlaceholder: "Замените пример ключом со страницы API-ключей.", batches: "Пакеты пополнения", codeInventory: "Реестр кодов", noRecords: "Записей пока нет." },
  tr: { proxy: "Proxy", proxyDescription: "Genel anahtar yalnızca izinli kenara ulaşır. Sağlayıcı bilgileri ve yönetim API'si özel kalır.", edgeReady: "Kenar hazır", upstreamNotProbed: "Özel üst kaynak sınanmadı", publicEndpoints: "Genel uç noktalar", adminDashboard: "Yönetici paneli", usersTotal: "Kullanıcılar", usersActive: "Etkin kullanıcılar", creditIssued: "Verilen kredi", batchesActive: "Etkin gruplar", proxySafety: "Proxy güvenliği", userReference: "Kullanıcı referansı", status: "Durum", auditTrail: "Denetim kaydı", createBatch: "Grup oluştur", importBatch: "Kodları içe aktar", adjustCredit: "Kredi ayarla", reason: "Neden", language: "Dil", accountConsole: "Hesap konsolu", quickSetup: "Hızlı kurulum", baseUrl: "Proxy temel URL", copy: "Kopyala", copied: "Kopyalandı", useKeyPlaceholder: "Örnekteki yer tutucuyu API anahtarları sayfasında oluşturduğunuz anahtarla değiştirin.", batches: "Yükleme kodu grupları", codeInventory: "Kod envanteri", noRecords: "Henüz kayıt yok." },
};

const securityTranslations = {
  en: { security: "Account security", activeSessions: "Active sessions", currentSession: "Current session", sessionStarted: "Started", sessionLastSeen: "Last active", sessionExpires: "Expires", logoutAll: "Sign out all devices", logoutAllHint: "This ends every hubapi browser session, including this one.", changePassword: "Change password", currentPassword: "Current password", newPassword: "New password", passwordChanged: "Password changed. Sign in again on every device.", sessionsEnded: "All sessions ended. Sign in again." },
  "zh-CN": { security: "账户安全", activeSessions: "活跃会话", currentSession: "当前会话", sessionStarted: "创建时间", sessionLastSeen: "最近活动", sessionExpires: "到期时间", logoutAll: "退出全部设备", logoutAllHint: "这会结束所有 hubapi 浏览器会话，包括当前会话。", changePassword: "修改密码", currentPassword: "当前密码", newPassword: "新密码", passwordChanged: "密码已修改，请在所有设备上重新登录。", sessionsEnded: "全部会话已退出，请重新登录。" },
  "zh-TW": { security: "帳戶安全", activeSessions: "作用中工作階段", currentSession: "目前工作階段", sessionStarted: "建立時間", sessionLastSeen: "最近活動", sessionExpires: "到期時間", logoutAll: "登出所有裝置", logoutAllHint: "這會結束所有 hubapi 瀏覽器工作階段，包括目前工作階段。", changePassword: "修改密碼", currentPassword: "目前密碼", newPassword: "新密碼", passwordChanged: "密碼已修改，請在所有裝置上重新登入。", sessionsEnded: "所有工作階段已結束，請重新登入。" },
  de: { security: "Kontosicherheit", activeSessions: "Aktive Sitzungen", currentSession: "Aktuelle Sitzung", sessionStarted: "Beginn", sessionLastSeen: "Zuletzt aktiv", sessionExpires: "Ablauf", logoutAll: "Alle Geräte abmelden", logoutAllHint: "Dies beendet alle hubapi-Browsersitzungen, einschließlich dieser.", changePassword: "Passwort ändern", currentPassword: "Aktuelles Passwort", newPassword: "Neues Passwort", passwordChanged: "Passwort geändert. Bitte auf allen Geräten neu anmelden.", sessionsEnded: "Alle Sitzungen beendet. Bitte erneut anmelden." },
  fr: { security: "Sécurité du compte", activeSessions: "Sessions actives", currentSession: "Session actuelle", sessionStarted: "Début", sessionLastSeen: "Dernière activité", sessionExpires: "Expiration", logoutAll: "Déconnecter tous les appareils", logoutAllHint: "Cette action ferme toutes les sessions hubapi, y compris celle-ci.", changePassword: "Modifier le mot de passe", currentPassword: "Mot de passe actuel", newPassword: "Nouveau mot de passe", passwordChanged: "Mot de passe modifié. Reconnectez-vous sur chaque appareil.", sessionsEnded: "Toutes les sessions sont fermées. Reconnectez-vous." },
  ja: { security: "アカウントセキュリティ", activeSessions: "有効なセッション", currentSession: "現在のセッション", sessionStarted: "開始", sessionLastSeen: "最終利用", sessionExpires: "有効期限", logoutAll: "すべての端末からログアウト", logoutAllHint: "現在を含むすべてのhubapiブラウザーセッションを終了します。", changePassword: "パスワード変更", currentPassword: "現在のパスワード", newPassword: "新しいパスワード", passwordChanged: "パスワードを変更しました。すべての端末で再ログインしてください。", sessionsEnded: "すべてのセッションを終了しました。再ログインしてください。" },
  ko: { security: "계정 보안", activeSessions: "활성 세션", currentSession: "현재 세션", sessionStarted: "시작", sessionLastSeen: "최근 활동", sessionExpires: "만료", logoutAll: "모든 기기에서 로그아웃", logoutAllHint: "현재 세션을 포함한 모든 hubapi 브라우저 세션을 종료합니다.", changePassword: "비밀번호 변경", currentPassword: "현재 비밀번호", newPassword: "새 비밀번호", passwordChanged: "비밀번호가 변경되었습니다. 모든 기기에서 다시 로그인하세요.", sessionsEnded: "모든 세션이 종료되었습니다. 다시 로그인하세요." },
  ru: { security: "Безопасность аккаунта", activeSessions: "Активные сеансы", currentSession: "Текущий сеанс", sessionStarted: "Начало", sessionLastSeen: "Последняя активность", sessionExpires: "Истекает", logoutAll: "Выйти на всех устройствах", logoutAllHint: "Будут завершены все сеансы hubapi, включая текущий.", changePassword: "Изменить пароль", currentPassword: "Текущий пароль", newPassword: "Новый пароль", passwordChanged: "Пароль изменён. Войдите заново на всех устройствах.", sessionsEnded: "Все сеансы завершены. Войдите снова." },
  tr: { security: "Hesap güvenliği", activeSessions: "Etkin oturumlar", currentSession: "Geçerli oturum", sessionStarted: "Başlangıç", sessionLastSeen: "Son etkinlik", sessionExpires: "Bitiş", logoutAll: "Tüm cihazlardan çıkış yap", logoutAllHint: "Geçerli oturum dahil tüm hubapi tarayıcı oturumlarını sonlandırır.", changePassword: "Parolayı değiştir", currentPassword: "Geçerli parola", newPassword: "Yeni parola", passwordChanged: "Parola değiştirildi. Tüm cihazlarda yeniden oturum açın.", sessionsEnded: "Tüm oturumlar sonlandırıldı. Yeniden oturum açın." },
};

const activityTranslations = {
  en: { requestHistory: "Request activity", noRequests: "No admitted requests yet.", routeLabel: "Route", modelLabel: "Model", reasonLabel: "Result", statusPending: "Pending", statusSettled: "Charged", statusReleased: "Released", creditOutstanding: "Outstanding credit", creditSettled: "Settled credit", pricing: "Accepted request price", fixedPricing: "Fixed per upstream-accepted request · version request-v1", requestPolicy: "Fixed charge when the private upstream accepts a request · version request-v1", passwordHint: "Use at least 12 characters; common and repeated passwords are rejected." },
  "zh-CN": { requestHistory: "请求记录", noRequests: "暂无已准入请求。", routeLabel: "路由", modelLabel: "模型", reasonLabel: "结果", statusPending: "处理中", statusSettled: "已扣费", statusReleased: "已释放", creditOutstanding: "当前未消费额度", creditSettled: "已结算额度", pricing: "已接受请求单价", fixedPricing: "私有上游接受请求后固定扣费 · 版本 request-v1", requestPolicy: "私有上游接受请求后固定扣费 · 版本 request-v1", passwordHint: "至少 12 个字符；常见弱密码和重复字符密码会被拒绝。" },
  "zh-TW": { requestHistory: "請求記錄", noRequests: "暫無已准入請求。", routeLabel: "路由", modelLabel: "模型", reasonLabel: "結果", statusPending: "處理中", statusSettled: "已扣費", statusReleased: "已釋放", creditOutstanding: "目前未消費額度", creditSettled: "已結算額度", pricing: "已接受請求單價", fixedPricing: "私有上游接受請求後固定扣費 · 版本 request-v1", requestPolicy: "私有上游接受請求後固定扣費 · 版本 request-v1", passwordHint: "至少 12 個字元；常見弱密碼和重複字元密碼會被拒絕。" },
  de: { requestHistory: "Anfrageaktivität", noRequests: "Noch keine angenommenen Anfragen.", routeLabel: "Route", modelLabel: "Modell", reasonLabel: "Ergebnis", statusPending: "Ausstehend", statusSettled: "Berechnet", statusReleased: "Freigegeben", creditOutstanding: "Ausstehendes Guthaben", creditSettled: "Abgerechnetes Guthaben", pricing: "Preis je angenommener Anfrage", fixedPricing: "Festpreis je vom Upstream angenommener Anfrage · Version request-v1", requestPolicy: "Festpreis, sobald der private Upstream eine Anfrage annimmt · Version request-v1", passwordHint: "Mindestens 12 Zeichen; häufige und wiederholte Passwörter werden abgelehnt." },
  fr: { requestHistory: "Activité des requêtes", noRequests: "Aucune requête admise.", routeLabel: "Route", modelLabel: "Modèle", reasonLabel: "Résultat", statusPending: "En attente", statusSettled: "Facturée", statusReleased: "Libérée", creditOutstanding: "Crédit restant", creditSettled: "Crédit réglé", pricing: "Prix par requête acceptée", fixedPricing: "Prix fixe par requête acceptée en amont · version request-v1", requestPolicy: "Frais fixes dès acceptation par l’amont privé · version request-v1", passwordHint: "Utilisez au moins 12 caractères ; les mots de passe courants ou répétés sont refusés." },
  ja: { requestHistory: "リクエスト履歴", noRequests: "承認済みリクエストはまだありません。", routeLabel: "ルート", modelLabel: "モデル", reasonLabel: "結果", statusPending: "処理中", statusSettled: "課金済み", statusReleased: "解放済み", creditOutstanding: "未使用クレジット", creditSettled: "決済済みクレジット", pricing: "受理済みリクエスト単価", fixedPricing: "アップストリーム受理時の固定料金 · request-v1", requestPolicy: "非公開アップストリームが受理した時点で固定課金 · request-v1", passwordHint: "12文字以上を使用してください。一般的または同一文字のパスワードは拒否されます。" },
  ko: { requestHistory: "요청 활동", noRequests: "승인된 요청이 아직 없습니다.", routeLabel: "경로", modelLabel: "모델", reasonLabel: "결과", statusPending: "처리 중", statusSettled: "과금됨", statusReleased: "해제됨", creditOutstanding: "미사용 크레딧", creditSettled: "정산 크레딧", pricing: "수락된 요청 단가", fixedPricing: "업스트림 수락 요청당 고정 요금 · request-v1", requestPolicy: "비공개 업스트림이 요청을 수락하면 고정 과금 · request-v1", passwordHint: "12자 이상을 사용하세요. 흔하거나 반복된 비밀번호는 거부됩니다." },
  ru: { requestHistory: "Активность запросов", noRequests: "Принятых запросов пока нет.", routeLabel: "Маршрут", modelLabel: "Модель", reasonLabel: "Результат", statusPending: "Ожидает", statusSettled: "Списано", statusReleased: "Освобождено", creditOutstanding: "Оставшийся кредит", creditSettled: "Списанный кредит", pricing: "Цена принятого запроса", fixedPricing: "Фиксированно за принятый upstream-запрос · request-v1", requestPolicy: "Фиксированное списание после принятия приватным upstream · request-v1", passwordHint: "Не менее 12 символов; распространённые и повторяющиеся пароли отклоняются." },
  tr: { requestHistory: "İstek etkinliği", noRequests: "Henüz kabul edilmiş istek yok.", routeLabel: "Rota", modelLabel: "Model", reasonLabel: "Sonuç", statusPending: "Bekliyor", statusSettled: "Ücretlendirildi", statusReleased: "Serbest bırakıldı", creditOutstanding: "Kalan kredi", creditSettled: "Kesinleşen kredi", pricing: "Kabul edilen istek fiyatı", fixedPricing: "Üst kaynakça kabul edilen istek başına sabit ücret · request-v1", requestPolicy: "Özel üst kaynak isteği kabul ettiğinde sabit ücret · request-v1", passwordHint: "En az 12 karakter kullanın; yaygın ve tekrarlanan parolalar reddedilir." },
};

const routingTranslations = {
  en: { taskRouting: "Task-routed models", taskRoutingHint: "Set model to a verified alias from the private routing core. hubapi passes it through unchanged and never classifies prompt text.", aliasExamples: "Verified available model aliases", catalogEmpty: "The private model catalog is available but empty.", catalogUnavailable: "The private model catalog is currently unavailable.", neverExpires: "No expiry" },
  "zh-CN": { taskRouting: "按任务路由模型", taskRoutingHint: "把 model 设为私有路由核心返回的已验证别名。hubapi 原样传递，不读取提示词正文进行分类。", aliasExamples: "已验证可用模型别名", catalogEmpty: "私有模型目录当前为空。", catalogUnavailable: "私有模型目录当前不可用。", neverExpires: "永不过期" },
  "zh-TW": { taskRouting: "按任務路由模型", taskRoutingHint: "把 model 設為私有路由核心回傳的已驗證別名。hubapi 原樣傳遞，不讀取提示詞正文進行分類。", aliasExamples: "已驗證可用模型別名", catalogEmpty: "私有模型目錄目前為空。", catalogUnavailable: "私有模型目錄目前無法使用。", neverExpires: "永不過期" },
  de: { taskRouting: "Aufgabenbasiertes Modellrouting", taskRoutingHint: "Setzen Sie model auf einen geprüften Alias des privaten Routing-Kerns. hubapi leitet ihn unverändert weiter und klassifiziert keine Prompts.", aliasExamples: "Geprüfte verfügbare Modell-Aliase", catalogEmpty: "Der private Modellkatalog ist verfügbar, aber leer.", catalogUnavailable: "Der private Modellkatalog ist derzeit nicht verfügbar.", neverExpires: "Kein Ablauf" },
  fr: { taskRouting: "Routage des modèles par tâche", taskRoutingHint: "Définissez model sur un alias vérifié par le cœur de routage privé. hubapi le transmet sans classer le texte du prompt.", aliasExamples: "Alias de modèles disponibles vérifiés", catalogEmpty: "Le catalogue privé est disponible, mais vide.", catalogUnavailable: "Le catalogue privé est actuellement indisponible.", neverExpires: "Sans expiration" },
  ja: { taskRouting: "タスク別モデルルーティング", taskRoutingHint: "model には非公開ルーティングコアが返す検証済みの別名を指定します。hubapi はそのまま渡し、プロンプト本文を分類しません。", aliasExamples: "検証済みの利用可能モデル別名", catalogEmpty: "非公開モデルカタログは利用可能ですが空です。", catalogUnavailable: "非公開モデルカタログは現在利用できません。", neverExpires: "有効期限なし" },
  ko: { taskRouting: "작업 기반 모델 라우팅", taskRoutingHint: "model을 비공개 라우팅 코어가 반환한 검증된 별칭으로 설정하세요. hubapi는 그대로 전달하며 프롬프트를 분류하지 않습니다.", aliasExamples: "검증된 사용 가능 모델 별칭", catalogEmpty: "비공개 모델 카탈로그가 비어 있습니다.", catalogUnavailable: "비공개 모델 카탈로그를 현재 사용할 수 없습니다.", neverExpires: "만료 없음" },
  ru: { taskRouting: "Маршрутизация моделей по задачам", taskRoutingHint: "Укажите в model проверенный псевдоним из приватного ядра маршрутизации. hubapi передаёт его без классификации текста запроса.", aliasExamples: "Проверенные доступные псевдонимы", catalogEmpty: "Приватный каталог доступен, но пуст.", catalogUnavailable: "Приватный каталог моделей сейчас недоступен.", neverExpires: "Без срока" },
  tr: { taskRouting: "Göreve göre model yönlendirme", taskRoutingHint: "model alanını özel yönlendirme çekirdeğinin doğruladığı bir takma ada ayarlayın. hubapi bunu değiştirmeden iletir ve istem metnini sınıflandırmaz.", aliasExamples: "Doğrulanmış kullanılabilir model takma adları", catalogEmpty: "Özel model kataloğu kullanılabilir ancak boş.", catalogUnavailable: "Özel model kataloğu şu anda kullanılamıyor.", neverExpires: "Süresiz" },
};

const app = /** @type {HTMLElement} */ (document.querySelector("#app"));
if (!(app instanceof HTMLElement)) throw new Error("hubapi app root is missing");

const supported = Object.keys(translations);
const browserLocale = navigator.language;
let locale = supported.includes(browserLocale) ? browserLocale : supported.find(value => browserLocale.startsWith(value.split("-")[0])) || "en";
let authMode = "login";
let activeView = "dashboard";
/** @type {{id:string,supportReference:string,role:"user"|"admin",status:string,createdAt:number}|null} */
let user = null;
let requestCostUnits = 100;
let pricingVersion = "unknown";
/** @type {{status:string,upstreamStatus:string,endpoints:string[]}} */
let proxyHealth = { status: "unknown", upstreamStatus: "not_probed", endpoints: [] };
let registrationEnabled = false;
let adminSelectedUserId = "";
let adminSelectedBatchId = "";

/** @param {string} key */
function t(key) {
  const current = /** @type {Record<string,string>} */ (translations[locale] || translations.en);
  const interfaces = /** @type {Record<string,Record<string,string>>} */ (interfaceTranslations);
  const currentInterface = interfaces[locale] || interfaces.en || {};
  const security = /** @type {Record<string,Record<string,string>>} */ (securityTranslations);
  const currentSecurity = security[locale] || security.en || {};
  const activities = /** @type {Record<string,Record<string,string>>} */ (activityTranslations);
  const currentActivity = activities[locale] || activities.en || {};
  const routing = /** @type {Record<string,Record<string,string>>} */ (routingTranslations);
  const currentRouting = routing[locale] || routing.en || {};
  return currentActivity[key] || currentRouting[key] || current[key] || currentInterface[key] || currentSecurity[key] || key;
}

/** @param {unknown} value */
function escapeHtml(value) {
  return String(value).replace(/[&<>'"]/g, character => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character] || character);
}

function csrfToken() {
  const pair = document.cookie.split("; ").find(value => value.startsWith("hubapi_csrf="));
  return pair?.slice("hubapi_csrf=".length) || "";
}

/** @param {string} path @param {RequestInit} [init] */
async function api(path, init = {}) {
  const headers = new Headers(init.headers);
  if (init.body && !headers.has("Content-Type")) headers.set("Content-Type", "application/json");
  if (init.method && init.method !== "GET") headers.set("x-hubapi-csrf-token", csrfToken());
  const response = await fetch(path, { ...init, headers, credentials: "same-origin" });
  const body = await response.json().catch(() => ({}));
  if (response.status === 401 && !path.endsWith("/login")) {
    user = null;
    authMode = "login";
    renderAuth(t("sessionExpired"));
    throw new Error("authentication_required");
  }
  if (!response.ok) throw new Error(typeof body.error === "string" ? body.error : "request_failed");
  return body;
}

/** @param {string} message @param {boolean} [error] */
function toast(message, error = false) {
  document.querySelector(".toast")?.remove();
  const node = document.createElement("div");
  node.className = `toast${error ? " danger" : ""}`;
  node.setAttribute("role", error ? "alert" : "status");
  node.textContent = message;
  document.body.append(node);
  setTimeout(() => node.remove(), 6000);
}

/** @param {string} value */
async function copyText(value) {
  if (navigator.clipboard?.writeText) {
    await navigator.clipboard.writeText(value);
    return;
  }
  const field = document.createElement("textarea");
  field.value = value;
  field.setAttribute("readonly", "");
  field.style.position = "fixed";
  field.style.opacity = "0";
  document.body.append(field);
  field.select();
  const copied = document.execCommand("copy");
  field.remove();
  if (!copied) throw new Error("clipboard_unavailable");
}

/** @param {string} [message] */
function renderAuth(message = "") {
  if (!registrationEnabled) authMode = "login";
  document.documentElement.lang = locale;
  app.innerHTML = `
    <main class="auth-shell" id="main">
      <section class="auth-story" aria-labelledby="auth-thesis">
        <a class="wordmark" href="/hub/">hub<i>api</i></a>
        <div class="auth-copy">
          <p class="eyebrow">${escapeHtml(t("tagline"))}</p>
          <h1 id="auth-thesis">${escapeHtml(t("headlineA"))} <span>${escapeHtml(t("headlineB"))}</span></h1>
          <p>${escapeHtml(t("description"))}</p>
        </div>
        <div class="route-rail" aria-label="${escapeHtml(t("tagline"))}"><b>${escapeHtml(t("client"))}</b><i></i><b>${escapeHtml(t("access"))}</b><i></i><b>${escapeHtml(t("providers"))}</b></div>
      </section>
      <section class="auth-panel">
        <div class="auth-card">
          <p class="eyebrow">${escapeHtml(t("accountConsole"))}</p>
          <h2>${escapeHtml(t("welcome"))}</h2>
          <div class="auth-tabs ${registrationEnabled ? "" : "single-tab"}" role="tablist">
            <button type="button" data-auth-mode="login" role="tab" aria-selected="${authMode === "login"}">${escapeHtml(t("signIn"))}</button>
            ${registrationEnabled ? `<button type="button" data-auth-mode="register" role="tab" aria-selected="${authMode === "register"}">${escapeHtml(t("register"))}</button>` : ""}
          </div>
          ${message ? `<div class="notice error" role="alert">${escapeHtml(message)}</div>` : ""}
          <form id="auth-form">
            <div class="field"><label for="email">${escapeHtml(t("email"))}</label><input id="email" name="email" type="email" autocomplete="email" required maxlength="254" /></div>
            <div class="field"><label for="password">${escapeHtml(t("password"))}</label><input id="password" name="password" type="password" autocomplete="${authMode === "login" ? "current-password" : "new-password"}" required minlength="12" maxlength="1024" /><small>${escapeHtml(t("passwordHint"))}</small></div>
            <button class="primary full" type="submit">${escapeHtml(t(authMode === "login" ? "signInButton" : "createAccount"))}</button>
          </form>
          <div class="field section-gap"><label for="auth-locale">${escapeHtml(t("language"))}</label>${localeSelect("auth-locale")}</div>
        </div>
      </section>
    </main>`;
  bindLocale();
  app.querySelectorAll("[data-auth-mode]").forEach(button => button.addEventListener("click", () => {
    authMode = button.getAttribute("data-auth-mode") || "login";
    renderAuth();
  }));
  app.querySelector("#auth-form")?.addEventListener("submit", async event => {
    event.preventDefault();
    const form = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    try {
      const result = await api(`/hub/auth/${authMode === "login" ? "login" : "register"}`, { method: "POST", body: JSON.stringify({ email: form.get("email"), password: form.get("password") }) });
      user = result.user;
      activeView = requestedView();
      renderConsole();
      await loadView();
    } catch {
      renderAuth(t("genericError"));
    }
  });
}

/** @param {string} id */
function localeSelect(id) {
  /** @type {Record<string, string>} */
  const labels = { en: "English", "zh-CN": "简体中文", "zh-TW": "繁體中文", de: "Deutsch", fr: "Français", ja: "日本語", ko: "한국어", ru: "Русский", tr: "Türkçe" };
  return `<select class="locale-select" id="${id}" aria-label="${escapeHtml(t("language"))}">${supported.map(value => `<option value="${value}" ${value === locale ? "selected" : ""}>${labels[value]}</option>`).join("")}</select>`;
}

function bindLocale() {
  app.querySelectorAll(".locale-select").forEach(select => select.addEventListener("change", event => {
    locale = /** @type {HTMLSelectElement} */ (event.currentTarget).value;
    user ? renderConsole() : renderAuth();
    if (user) loadView();
  }));
}

function requestedView() {
  const requested = location.hash.replace(/^#/, "");
  const allowed = ["dashboard", "proxy", "keys", "recharge", "security", ...(user?.role === "admin" ? ["admin"] : [])];
  return allowed.includes(requested) ? requested : "dashboard";
}

function renderConsole() {
  if (!user) return renderAuth();
  document.documentElement.lang = locale;
  const nav = [
    ["dashboard", t("dashboard")], ["proxy", t("proxy")], ["keys", t("keys")], ["recharge", t("recharge")], ["security", t("security")],
    ...(user.role === "admin" ? [["admin", t("adminDashboard")]] : []),
  ];
  app.innerHTML = `
    <div class="console">
      <aside class="side">
        <a class="wordmark" href="/hub/">hub<i>api</i></a>
        <nav aria-label="${escapeHtml(t("accountConsole"))}">${nav.map(([id, label]) => `<a href="#${id}" class="nav-button ${activeView === id ? "active" : ""}" data-view="${id}">${escapeHtml(label)}</a>`).join("")}</nav>
        <div class="side-foot">${localeSelect("console-locale")}<button class="quiet" id="logout">${escapeHtml(t("logout"))}</button></div>
      </aside>
      <main class="workspace" id="main">
        <header class="workspace-head"><div><p class="eyebrow">${escapeHtml(t("live"))}</p><h1>${escapeHtml(t(activeView === "admin" ? "adminDashboard" : activeView))}</h1></div><div class="identity"><strong>hubapi</strong><span>${escapeHtml(t(user.role === "admin" ? "roleAdmin" : "roleUser"))}</span></div></header>
        <div id="view"><div class="empty">${escapeHtml(t("refresh"))}…</div></div>
      </main>
    </div>`;
  bindLocale();
  app.querySelectorAll("[data-view]").forEach(button => button.addEventListener("click", event => {
    event.preventDefault();
    activeView = button.getAttribute("data-view") || "dashboard";
    history.pushState(null, "", `#${activeView}`);
    renderConsole();
    loadView();
  }));
  app.querySelector("#logout")?.addEventListener("click", async () => {
    try { await api("/hub/auth/logout", { method: "POST" }); } catch { /* session is cleared locally below */ }
    user = null;
    authMode = "login";
    renderAuth();
  });
}

async function loadView() {
  const view = app.querySelector("#view");
  if (!(view instanceof HTMLElement) || !user) return;
  try {
    if (activeView === "dashboard") await renderDashboard(view);
    else if (activeView === "proxy") await renderProxy(view);
    else if (activeView === "keys") await renderKeys(view);
    else if (activeView === "recharge") await renderRecharge(view);
    else if (activeView === "security") await renderSecurity(view);
    else if (activeView === "admin" && user.role === "admin") await renderAdmin(view);
  } catch { view.innerHTML = `<div class="notice error">${escapeHtml(t("genericError"))}</div>`; }
}

/** @param {HTMLElement} view */
async function renderProxy(view) {
  const health = await api("/hub/health");
  proxyHealth = health.proxy;
  requestCostUnits = health.requestCostUnits;
  pricingVersion = health.pricingVersion;
  const methods = /** @type {Record<string,string>} */ ({ "/v1/models": "GET", "/v1/responses": "POST", "/v1/chat/completions": "POST", "/v1/messages": "POST" });
  const proxyOrigin = location.origin;
  const publicBaseUrl = `${proxyOrigin}/v1`;
  const transportLabel = location.protocol === "https:" ? "HTTPS / Bearer" : "HTTP · DEV / Bearer";
  const keyPlaceholder = "hub_live_REPLACE_ME";
  const recipes = [
    { title: "OpenAI SDK", value: `export OPENAI_BASE_URL="${publicBaseUrl}"\nexport OPENAI_API_KEY="${keyPlaceholder}"` },
    { title: "Codex CLI · config.toml", value: `# Use an alias shown in the verified model list\nmodel = "YOUR_CONFIGURED_ALIAS"\nmodel_provider = "hubapi"\n\n[model_providers.hubapi]\nname = "hubapi"\nbase_url = "${publicBaseUrl}"\nwire_api = "responses"\nrequires_openai_auth = true\nenv_key = "HUBAPI_KEY"\n\n# shell\nexport HUBAPI_KEY="${keyPlaceholder}"` },
    { title: "Claude Code", value: `export ANTHROPIC_BASE_URL="${proxyOrigin}"\nexport ANTHROPIC_AUTH_TOKEN="${keyPlaceholder}"\nclaude` },
  ];
  view.innerHTML = `
    <section class="pixel-route" aria-label="${escapeHtml(t("proxy"))}">
      <div class="pixel-node"><span>01</span><strong>${escapeHtml(t("client"))}</strong><small>hub_live_••••</small></div>
      <div class="pixel-pipe" aria-hidden="true"><i></i></div>
      <div class="pixel-node active"><span>02</span><strong>${escapeHtml(t("access"))}</strong><small>${escapeHtml(t("edgeReady"))}</small></div>
      <div class="pixel-pipe" aria-hidden="true"><i></i></div>
      <div class="pixel-node"><span>03</span><strong>${escapeHtml(t("providers"))}</strong><small>${escapeHtml(t("upstreamNotProbed"))}</small></div>
    </section>
    <section class="grid-two proxy-grid">
      <article class="panel pixel-panel"><p class="eyebrow">${escapeHtml(t("publicEndpoints"))}</p><h2>${escapeHtml(t("proxy"))}</h2><p class="muted">${escapeHtml(t("proxyDescription"))}</p><div class="endpoint-list">${proxyHealth.endpoints.map(path => `<div class="endpoint-row"><span class="method">${methods[path] || "POST"}</span><code>${escapeHtml(path)}</code><span class="status-chip good">${escapeHtml(t("edgeReady"))}</span></div>`).join("")}</div></article>
      <article class="panel pixel-panel"><p class="eyebrow">${escapeHtml(t("pricing"))}</p><div class="proxy-price"><strong>${escapeHtml(requestCostUnits)}</strong><span>${escapeHtml(t("creditUnit"))}</span></div><dl class="fact-list"><div><dt>${escapeHtml(t("status"))}</dt><dd>${escapeHtml(proxyHealth.status)}</dd></div><div><dt>pricingVersion</dt><dd class="mono">${escapeHtml(pricingVersion)}</dd></div><div><dt>Authorization</dt><dd class="mono">Bearer hub_live_••••</dd></div><div><dt>${escapeHtml(t("providers"))}</dt><dd>${escapeHtml(t("upstreamNotProbed"))}</dd></div></dl></article>
    </section>
    <section class="panel pixel-panel section-gap-small">
      <div class="panel-head"><div><p class="eyebrow">model contract</p><h2>${escapeHtml(t("taskRouting"))}</h2></div><span class="status-chip">hubapi</span></div>
      <p class="muted">${escapeHtml(t("taskRoutingHint"))}</p>
      <div id="model-catalog"><div class="empty">${escapeHtml(t("refresh"))}…</div></div>
    </section>
    <section class="panel pixel-panel proxy-setup section-gap-small">
      <div class="panel-head"><div><p class="eyebrow">${escapeHtml(t("quickSetup"))}</p><h2>${escapeHtml(t("baseUrl"))}</h2></div><button class="secondary compact-button" type="button" data-copy-base>${escapeHtml(t("copy"))}</button></div>
      <div class="base-url-row"><code>${escapeHtml(publicBaseUrl)}</code><span class="status-chip good">${escapeHtml(transportLabel)}</span></div>
      <p class="muted">${escapeHtml(t("useKeyPlaceholder"))}</p>
      <div class="recipe-grid">${recipes.map((recipe, index) => `<article class="recipe-card"><div class="recipe-head"><strong>${escapeHtml(recipe.title)}</strong><button class="quiet copy-button" type="button" data-copy-recipe="${index}">${escapeHtml(t("copy"))}</button></div><pre><code>${escapeHtml(recipe.value)}</code></pre></article>`).join("")}</div>
    </section>`;
  view.querySelector("[data-copy-base]")?.addEventListener("click", async () => {
    try { await copyText(publicBaseUrl); toast(t("copied")); } catch { toast(t("genericError"), true); }
  });
  view.querySelectorAll("[data-copy-recipe]").forEach(button => button.addEventListener("click", async () => {
    const index = Number(button.getAttribute("data-copy-recipe"));
    const recipe = recipes[index];
    if (!recipe) return;
    try { await copyText(recipe.value); toast(t("copied")); } catch { toast(t("genericError"), true); }
  }));
  void renderModelCatalog(view);
}

/** @param {HTMLElement} view */
async function renderModelCatalog(view) {
  const target = view.querySelector("#model-catalog");
  if (!(target instanceof HTMLElement)) return;
  try {
    const result = await api("/hub/account/models");
    if (!target.isConnected) return;
    const rawModels = /** @type {unknown[]} */ (Array.isArray(result.models) ? result.models : []);
    const models = rawModels.filter(value => typeof value === "string");
    const observedAt = Number(result.observedAt);
    const upstream = Number.isSafeInteger(result.upstreamStatus) ? `HTTP ${result.upstreamStatus}` : "N/A";
    if (result.status === "available" && models.length) {
      target.innerHTML = `<div class="panel-head section-gap-small"><small>${escapeHtml(t("aliasExamples"))}</small><span class="status-chip good">${escapeHtml(upstream)} · ${escapeHtml(Number.isFinite(observedAt) ? new Date(observedAt).toLocaleString(locale) : "N/A")}</span></div><div class="endpoint-list">${models.map(model => `<div class="endpoint-row"><span class="method">model</span><code>${escapeHtml(model)}</code><span class="status-chip good">${escapeHtml(t("available"))}</span></div>`).join("")}</div>`;
      return;
    }
    target.innerHTML = `<div class="notice ${result.status === "empty" ? "" : "error"}">${escapeHtml(t(result.status === "empty" ? "catalogEmpty" : "catalogUnavailable"))} · ${escapeHtml(upstream)}</div>`;
  } catch {
    if (target.isConnected) target.innerHTML = `<div class="notice error">${escapeHtml(t("catalogUnavailable"))} · N/A</div>`;
  }
}

/** @param {HTMLElement} view */
async function renderDashboard(view) {
  const [balance, ledger, keys, requestResult] = await Promise.all([
    api("/hub/account/balance"),
    api("/hub/account/ledger?limit=6"),
    api("/hub/account/keys"),
    api("/hub/account/requests?limit=6"),
  ]);
  view.innerHTML = `
    <section class="passage" aria-label="Request passage"><div class="passage-step live"><b>${escapeHtml(t("passageClient"))}</b><span>${keys.keys.length} ${escapeHtml(t("keys"))}</span></div><div class="passage-step live"><b>${escapeHtml(t("passageAdmission"))}</b><span>${escapeHtml(balance.availableUnits)} ${escapeHtml(t("creditUnit"))}</span></div><div class="passage-step live"><b>${escapeHtml(t("passageRoute"))}</b><span>hubapi · private</span></div></section>
    <section class="metrics"><article class="metric primary-metric"><label>${escapeHtml(t("balance"))}</label><strong>${escapeHtml(balance.balanceUnits)}</strong><small>${escapeHtml(t("creditUnit"))}</small></article><article class="metric"><label>${escapeHtml(t("available"))}</label><strong>${escapeHtml(balance.availableUnits)}</strong><small>${escapeHtml(t("creditUnit"))}</small></article><article class="metric"><label>${escapeHtml(t("reserved"))}</label><strong>${escapeHtml(balance.reservedUnits)}</strong><small>${escapeHtml(t("creditUnit"))}</small></article></section>
    <section class="grid-two"><article class="panel"><div class="panel-head"><h2>${escapeHtml(t("ledger"))}</h2><button class="quiet" id="refresh-dashboard">${escapeHtml(t("refresh"))}</button></div>${ledgerRows(ledger.entries)}</article><article class="panel"><div class="panel-head"><h2>${escapeHtml(t("requestHistory"))}</h2></div>${requestRows(requestResult.requests)}</article></section>
    <section class="panel section-gap-small"><p class="eyebrow">${escapeHtml(t("pricing"))}</p><h2>${escapeHtml(requestCostUnits)} ${escapeHtml(t("creditUnit"))}</h2><p class="muted">${escapeHtml(t("requestPolicy").replace("request-v1", pricingVersion))}</p></section>`;
  view.querySelector("#refresh-dashboard")?.addEventListener("click", () => renderDashboard(view));
}

/** @param {Array<Record<string,unknown>>} entries */
function ledgerRows(entries) {
  if (!entries.length) return `<div class="empty">${escapeHtml(t("noLedger"))}</div>`;
  return `<div class="list">${entries.map(entry => `<div class="row"><div class="row-main"><strong>${escapeHtml(entry.kind)}</strong><small>${escapeHtml(entry.reason)} · ${escapeHtml(new Date(Number(entry.createdAt)).toLocaleString(locale))}</small></div><span class="amount ${Number(entry.amountUnits) < 0 ? "negative" : ""}">${Number(entry.amountUnits) > 0 ? "+" : ""}${escapeHtml(entry.amountUnits)}</span></div>`).join("")}</div>`;
}

/** @param {Array<Record<string,unknown>>} entries @param {boolean} [showUser] */
function requestRows(entries, showUser = false) {
  if (!entries.length) return `<div class="empty">${escapeHtml(t("noRequests"))}</div>`;
  return `<div class="list">${entries.map(entry => {
    const units = entry.settledUnits === null ? entry.reservedUnits : entry.settledUnits;
    const identity = showUser && entry.supportReference ? `${entry.supportReference} · ` : "";
    const model = entry.modelAlias || "N/A";
    const route = entry.routePath || "N/A";
    const upstream = entry.upstreamStatus === null ? "N/A" : `HTTP ${entry.upstreamStatus}`;
    const terminalReason = entry.terminalReason || "N/A";
    const statusLabel = entry.status === "settled" ? t("statusSettled") : entry.status === "released" ? t("statusReleased") : t("statusPending");
    return `<div class="row"><div class="row-main"><strong>${escapeHtml(identity)}${escapeHtml(t("modelLabel"))}: ${escapeHtml(model)}</strong><small>${escapeHtml(t("routeLabel"))}: ${escapeHtml(route)} · ${escapeHtml(upstream)} · ${escapeHtml(t("reasonLabel"))}: ${escapeHtml(terminalReason)} · ${escapeHtml(new Date(Number(entry.createdAt)).toLocaleString(locale))}</small></div><span class="status-chip ${entry.status === "settled" ? "good" : ""}">${escapeHtml(statusLabel)} · ${escapeHtml(units)} ${escapeHtml(t("creditUnit"))}</span></div>`;
  }).join("")}</div>`;
}

/** @param {HTMLElement} view */
async function renderKeys(view) {
  const result = await api("/hub/account/keys");
  const keys = /** @type {Array<Record<string, unknown>>} */ (result.keys);
  view.innerHTML = `<section class="grid-two"><article class="panel"><div class="panel-head"><h2>${escapeHtml(t("myKeys"))}</h2></div>${keys.length ? `<div class="list">${keys.map(key => `<div class="row"><div class="row-main"><strong>${escapeHtml(key.name)}</strong><small class="mono">${escapeHtml(key.prefix)}••••${escapeHtml(key.suffix)}</small></div>${key.status === "active" ? `<button class="danger-button" data-revoke="${escapeHtml(key.id)}">${escapeHtml(t("revoke"))}</button>` : `<span class="muted">${escapeHtml(key.status)}</span>`}</div>`).join("")}</div>` : `<div class="empty">${escapeHtml(t("noKeys"))}</div>`}</article><article class="panel"><h2>${escapeHtml(t("createKey"))}</h2><form class="action-form" id="create-key"><div class="field"><label for="key-name">${escapeHtml(t("keyName"))}</label><input id="key-name" name="name" required maxlength="80" /></div><button class="primary" type="submit">${escapeHtml(t("createKey"))}</button></form><div id="key-reveal"></div></article></section>`;
  view.querySelector("#create-key")?.addEventListener("submit", async event => {
    event.preventDefault();
    const data = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    try {
      const created = await api("/hub/account/keys", { method: "POST", body: JSON.stringify({ name: data.get("name") }) });
      const reveal = view.querySelector("#key-reveal");
      if (reveal) reveal.innerHTML = `<div class="notice"><strong>${escapeHtml(t("revealed"))}</strong><p class="secret-reveal mono">${escapeHtml(created.key.key)}</p><small>${escapeHtml(t("saveNow"))}</small></div>`;
    } catch { toast(t("genericError"), true); }
  });
  view.querySelectorAll("[data-revoke]").forEach(button => button.addEventListener("click", async () => {
    try { await api("/hub/account/key", { method: "DELETE", body: JSON.stringify({ keyId: button.getAttribute("data-revoke") }) }); await renderKeys(view); } catch { toast(t("genericError"), true); }
  }));
}

/** @param {HTMLElement} view */
async function renderRecharge(view) {
  const [balance, ledger] = await Promise.all([api("/hub/account/balance"), api("/hub/account/ledger?limit=20")]);
  view.innerHTML = `<section class="grid-two"><article class="panel"><p class="eyebrow">${escapeHtml(t("balance"))}</p><div class="metric primary-metric"><strong>${escapeHtml(balance.balanceUnits)}</strong><small>${escapeHtml(t("creditUnit"))}</small></div><h2 class="section-gap">${escapeHtml(t("redeemTitle"))}</h2><form class="action-form" id="redeem-form"><div class="field"><label for="recharge-code">${escapeHtml(t("code"))}</label><input class="mono" id="recharge-code" name="code" required autocomplete="off" /></div><button class="primary" type="submit">${escapeHtml(t("redeem"))}</button></form></article><article class="panel"><div class="panel-head"><h2>${escapeHtml(t("ledger"))}</h2></div>${ledgerRows(ledger.entries)}</article></section>`;
  view.querySelector("#redeem-form")?.addEventListener("submit", async event => {
    event.preventDefault();
    const data = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    try {
      await api("/hub/account/redeem", { method: "POST", headers: { "Idempotency-Key": crypto.randomUUID() }, body: JSON.stringify({ code: data.get("code") }) });
      toast(t("redeem"));
      await renderRecharge(view);
    } catch { toast(t("genericError"), true); }
  });
}

/** @param {HTMLElement} view */
async function renderSecurity(view) {
  const result = await api("/hub/auth/sessions");
  const sessions = /** @type {Array<Record<string, unknown>>} */ (result.sessions);
  const currentSessionId = String(result.currentSessionId || "");
  /** @param {unknown} value */
  const formatTime = value => new Date(Number(value)).toLocaleString(locale);
  view.innerHTML = `<section class="grid-two">
    <article class="panel"><h2>${escapeHtml(t("accountConsole"))}</h2><dl class="fact-list section-gap-small"><div><dt>${escapeHtml(t("userReference"))}</dt><dd class="mono">${escapeHtml(user?.supportReference || "N/A")}</dd></div><div><dt>${escapeHtml(t("status"))}</dt><dd>${escapeHtml(user?.status || "N/A")}</dd></div><div><dt>${escapeHtml(t("sessionStarted"))}</dt><dd>${escapeHtml(user?.createdAt ? formatTime(user.createdAt) : "N/A")}</dd></div></dl><div class="panel-head section-gap"><h2>${escapeHtml(t("activeSessions"))}</h2><span class="status-chip good">${sessions.length}</span></div>${sessions.length ? `<div class="list">${sessions.map(session => `<div class="row"><div class="row-main"><strong>${session.id === currentSessionId ? escapeHtml(t("currentSession")) : "hubapi session"}</strong><small>${escapeHtml(t("sessionStarted"))}: ${escapeHtml(formatTime(session.createdAt))}<br>${escapeHtml(t("sessionLastSeen"))}: ${escapeHtml(formatTime(session.lastSeenAt))}<br>${escapeHtml(t("sessionExpires"))}: ${escapeHtml(formatTime(session.expiresAt))}</small></div></div>`).join("")}</div>` : `<div class="empty">${escapeHtml(t("sessionExpired"))}</div>`}</article>
    <article class="panel"><h2>${escapeHtml(t("changePassword"))}</h2><form class="action-form section-gap-small" id="password-form"><div class="field"><label for="current-password">${escapeHtml(t("currentPassword"))}</label><input id="current-password" name="currentPassword" type="password" autocomplete="current-password" required maxlength="1024" /></div><div class="field"><label for="new-password">${escapeHtml(t("newPassword"))}</label><input id="new-password" name="newPassword" type="password" autocomplete="new-password" required minlength="12" maxlength="1024" /><small>${escapeHtml(t("passwordHint"))}</small></div><button class="primary" type="submit">${escapeHtml(t("changePassword"))}</button></form><div class="danger-zone section-gap"><p>${escapeHtml(t("logoutAllHint"))}</p><button class="danger-button" type="button" id="logout-all">${escapeHtml(t("logoutAll"))}</button></div></article>
  </section>`;
  view.querySelector("#password-form")?.addEventListener("submit", async event => {
    event.preventDefault();
    const data = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    try {
      await api("/hub/auth/password", { method: "POST", body: JSON.stringify({ currentPassword: data.get("currentPassword"), newPassword: data.get("newPassword") }) });
      user = null;
      authMode = "login";
      renderAuth(t("passwordChanged"));
    } catch { toast(t("genericError"), true); }
  });
  view.querySelector("#logout-all")?.addEventListener("click", async () => {
    try {
      await api("/hub/auth/logout-all", { method: "POST" });
      user = null;
      authMode = "login";
      renderAuth(t("sessionsEnded"));
    } catch { toast(t("genericError"), true); }
  });
}

/** @param {HTMLElement} view */
async function renderAdmin(view) {
  const [usersResult, batchesResult, auditResult, health, metrics, requestResult] = await Promise.all([
    api("/hub/admin/users"),
    api("/hub/admin/recharge-batches"),
    api("/hub/admin/audit"),
    api("/hub/health"),
    api("/hub/admin/metrics"),
    api("/hub/admin/requests?limit=20"),
  ]);
  const users = /** @type {Array<Record<string, unknown>>} */ (usersResult.users);
  const batches = /** @type {Array<Record<string, unknown>>} */ (batchesResult.batches);
  const events = /** @type {Array<Record<string, unknown>>} */ (auditResult.events);
  const requests = /** @type {Array<Record<string, unknown>>} */ (requestResult.requests);
  /** @param {Record<string, unknown>} row */
  const userLabel = row => String(row.supportReference || "N/A");
  if (!users.some(row => row.id === adminSelectedUserId)) adminSelectedUserId = String(users[0]?.id || "");
  if (!batches.some(row => row.id === adminSelectedBatchId)) adminSelectedBatchId = String(batches[0]?.id || "");
  const [detailResult, codeResult] = await Promise.all([
    adminSelectedUserId ? api("/hub/admin/user-details", { method: "POST", body: JSON.stringify({ userId: adminSelectedUserId, ledgerLimit: 30 }) }) : Promise.resolve({ keys: [], ledger: [] }),
    adminSelectedBatchId ? api("/hub/admin/recharge-code-inventory", { method: "POST", body: JSON.stringify({ batchId: adminSelectedBatchId }) }) : Promise.resolve({ codes: [] }),
  ]);
  const managedKeys = /** @type {Array<Record<string, unknown>>} */ (detailResult.keys || []);
  const managedLedger = /** @type {Array<Record<string, unknown>>} */ (detailResult.ledger || []);
  const rechargeCodes = /** @type {Array<Record<string, unknown>>} */ (codeResult.codes || []);
  const expiryInputMin = new Date(Date.now() + 60_000 - new Date().getTimezoneOffset() * 60_000).toISOString().slice(0, 16);
  /** @param {unknown} value */
  const expiryLabel = value => value === null || value === undefined ? t("neverExpires") : new Date(Number(value)).toLocaleString(locale);
  view.innerHTML = `
    <section class="admin-kpis">
      <article class="metric pixel-metric"><label>${escapeHtml(t("usersTotal"))}</label><strong>${escapeHtml(metrics.usersTotal)}</strong><small>${escapeHtml(t("status"))}</small></article>
      <article class="metric pixel-metric"><label>${escapeHtml(t("usersActive"))}</label><strong>${escapeHtml(metrics.activeUsers)}</strong><small>${escapeHtml(t("edgeReady"))}</small></article>
      <article class="metric pixel-metric"><label>${escapeHtml(t("creditIssued"))}</label><strong>${escapeHtml(metrics.issuedUnits)}</strong><small>${escapeHtml(t("creditUnit"))}</small></article>
      <article class="metric pixel-metric"><label>${escapeHtml(t("creditOutstanding"))}</label><strong>${escapeHtml(metrics.outstandingUnits)}</strong><small>${escapeHtml(t("creditUnit"))}</small></article>
    </section>
    <section class="grid-two">
      <article class="panel pixel-panel">
        <div class="panel-head"><div><p class="eyebrow">${escapeHtml(t("proxySafety"))}</p><h2>${escapeHtml(t("proxy"))}</h2></div><span class="status-chip good">${escapeHtml(t("edgeReady"))}</span></div>
        <p class="muted">${escapeHtml(t("proxyDescription"))}</p>
        <dl class="fact-list"><div><dt>${escapeHtml(t("publicEndpoints"))}</dt><dd>${health.proxy.endpoints.length}</dd></div><div><dt>pricingVersion</dt><dd class="mono">${escapeHtml(health.pricingVersion)}</dd></div><div><dt>${escapeHtml(t("pricing"))}</dt><dd>${escapeHtml(health.requestCostUnits)} ${escapeHtml(t("creditUnit"))}</dd></div><div><dt>${escapeHtml(t("creditSettled"))}</dt><dd>${escapeHtml(metrics.settledUnits)} ${escapeHtml(t("creditUnit"))}</dd></div><div><dt>${escapeHtml(t("providers"))}</dt><dd>${escapeHtml(t("upstreamNotProbed"))}</dd></div></dl>
      </article>
      <article class="panel pixel-panel">
        <h2>${escapeHtml(t("usersTotal"))}</h2>
        <div class="list">${users.map(row => `<div class="row"><div class="row-main"><strong class="mono">${escapeHtml(userLabel(row))}</strong><small>${escapeHtml(row.role)} · ${escapeHtml(row.balanceUnits)} ${escapeHtml(t("creditUnit"))} · ${escapeHtml(row.status)}</small></div>${row.id !== user?.id ? `<button class="secondary" data-user-status="${escapeHtml(row.id)}" data-next-status="${row.status === "active" ? "disabled" : "active"}">${escapeHtml(row.status === "active" ? t("revoke") : t("refresh"))}</button>` : ""}</div>`).join("")}</div>
        <h2 class="section-gap">${escapeHtml(t("adjustCredit"))}</h2>
        <form class="action-form" id="adjust-form"><div class="field"><label for="adjust-user">${escapeHtml(t("userReference"))}</label><select id="adjust-user" name="userId">${users.map(row => `<option value="${escapeHtml(row.id)}">${escapeHtml(userLabel(row))}</option>`).join("")}</select></div><div class="field"><label for="adjust-units">${escapeHtml(t("creditUnit"))}</label><input id="adjust-units" name="amountUnits" type="number" step="1" required /></div><div class="field"><label for="adjust-reason">${escapeHtml(t("reason"))}</label><input id="adjust-reason" name="reason" required maxlength="160" /></div><button class="primary" type="submit">${escapeHtml(t("adjustCredit"))}</button></form>
      </article>
    </section>
    <section class="grid-two section-gap-small">
      <article class="panel pixel-panel"><h2>${escapeHtml(t("createBatch"))}</h2><form class="action-form" id="batch-form"><div class="field"><label for="batch-label">${escapeHtml(t("label"))}</label><input id="batch-label" name="label" required maxlength="120" /></div><div class="field"><label for="batch-units">${escapeHtml(t("units"))}</label><input id="batch-units" name="unitAmount" type="number" min="1" step="1" required /></div><div class="field"><label for="batch-quantity">${escapeHtml(t("quantity"))}</label><input id="batch-quantity" name="quantity" type="number" min="1" max="1000" step="1" required /></div><div class="field"><label for="batch-expiry">${escapeHtml(t("sessionExpires"))}</label><input id="batch-expiry" name="expiresAt" type="datetime-local" min="${expiryInputMin}" /></div><button class="primary" type="submit">${escapeHtml(t("generate"))}</button></form><div id="batch-reveal"></div><h2 class="section-gap">${escapeHtml(t("importBatch"))}</h2><form class="action-form" id="import-form"><div class="field"><label for="import-label">${escapeHtml(t("label"))}</label><input id="import-label" name="label" required maxlength="120" /></div><div class="field"><label for="import-units">${escapeHtml(t("units"))}</label><input id="import-units" name="unitAmount" type="number" min="1" step="1" required /></div><div class="field"><label for="import-expiry">${escapeHtml(t("sessionExpires"))}</label><input id="import-expiry" name="expiresAt" type="datetime-local" min="${expiryInputMin}" /></div><div class="field"><label for="import-codes">${escapeHtml(t("code"))}</label><textarea id="import-codes" name="codes" class="mono" required></textarea></div><button class="secondary" type="submit">${escapeHtml(t("importBatch"))}</button></form></article>
      <article class="panel pixel-panel"><div class="panel-head"><h2>${escapeHtml(t("batches"))}</h2><span class="status-chip good">${escapeHtml(metrics.activeBatches)} ${escapeHtml(t("batchesActive"))}</span></div>${batches.length ? `<div class="list">${batches.map(row => `<div class="row"><div class="row-main"><strong>${escapeHtml(row.label)}</strong><small>${escapeHtml(row.unitAmount)} × ${escapeHtml(row.quantity)} · ${escapeHtml(row.available)} ${escapeHtml(t("available"))} · ${escapeHtml(row.redeemed)} ${escapeHtml(t("recharge"))}<br>${escapeHtml(t("sessionExpires"))}: ${escapeHtml(expiryLabel(row.expiresAt))}</small></div>${row.status === "active" ? `<button class="danger-button" data-revoke-batch="${escapeHtml(row.id)}">${escapeHtml(t("revoke"))}</button>` : `<span class="muted">${escapeHtml(row.status)}</span>`}</div>`).join("")}</div>` : `<div class="empty">${escapeHtml(t("noRecords"))}</div>`}</article>
    </section>
    <section class="grid-two section-gap-small">
      <article class="panel pixel-panel"><div class="panel-head"><h2>${escapeHtml(t("keys"))}</h2><select id="admin-user-detail" aria-label="${escapeHtml(t("userReference"))}">${users.map(row => `<option value="${escapeHtml(row.id)}" ${row.id === adminSelectedUserId ? "selected" : ""}>${escapeHtml(userLabel(row))}</option>`).join("")}</select></div>${managedKeys.length ? `<div class="list">${managedKeys.map(key => `<div class="row"><div class="row-main"><strong>${escapeHtml(key.name)}</strong><small class="mono">${escapeHtml(key.prefix)}••••${escapeHtml(key.suffix)} · ${escapeHtml(key.status)}</small></div>${key.status === "active" ? `<button class="danger-button" data-admin-revoke-key="${escapeHtml(key.id)}">${escapeHtml(t("revoke"))}</button>` : ""}</div>`).join("")}</div>` : `<div class="empty">${escapeHtml(t("noKeys"))}</div>`}<h2 class="section-gap">${escapeHtml(t("ledger"))}</h2>${ledgerRows(managedLedger)}</article>
      <article class="panel pixel-panel"><div class="panel-head"><h2>${escapeHtml(t("codeInventory"))}</h2><select id="admin-batch-detail" aria-label="${escapeHtml(t("codeInventory"))}">${batches.map(row => `<option value="${escapeHtml(row.id)}" ${row.id === adminSelectedBatchId ? "selected" : ""}>${escapeHtml(row.label)}</option>`).join("")}</select></div>${rechargeCodes.length ? `<div class="list">${rechargeCodes.map(codeRow => `<div class="row"><div class="row-main"><strong class="mono">hub_rc_••••${escapeHtml(codeRow.suffix)}</strong><small>${escapeHtml(t("status"))}: ${escapeHtml(codeRow.status)} · ${escapeHtml(t("userReference"))}: ${escapeHtml(codeRow.redeemedBy || "N/A")}</small></div><span class="status-chip ${codeRow.status === "redeemed" ? "good" : ""}">${escapeHtml(codeRow.status)}</span></div>`).join("")}</div>` : `<div class="empty">${escapeHtml(t("noRecords"))}</div>`}</article>
    </section>
    <section class="grid-two section-gap-small"><article class="panel pixel-panel"><h2>${escapeHtml(t("requestHistory"))}</h2>${requestRows(requests, true)}</article><article class="panel pixel-panel"><h2>${escapeHtml(t("auditTrail"))}</h2>${events.length ? `<div class="list">${events.slice(0, 20).map(row => `<div class="row"><div class="row-main"><strong>${escapeHtml(row.action)}</strong><small>${escapeHtml(row.targetType)} · ${escapeHtml(row.outcome)}</small></div><span class="mono muted">${escapeHtml(new Date(Number(row.createdAt)).toLocaleString(locale))}</span></div>`).join("")}</div>` : `<div class="empty">${escapeHtml(t("noRecords"))}</div>`}</article></section>`;
  view.querySelector("#batch-form")?.addEventListener("submit", async event => {
    event.preventDefault();
    const data = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    try {
      const expiryValue = String(data.get("expiresAt") || "");
      const result = await api("/hub/admin/recharge-batches", { method: "POST", body: JSON.stringify({ label: data.get("label"), unitAmount: Number(data.get("unitAmount")), quantity: Number(data.get("quantity")), expiresAt: expiryValue ? new Date(expiryValue).getTime() : null }) });
      const reveal = view.querySelector("#batch-reveal");
      if (reveal) reveal.className = "secret-reveal mono";
      if (reveal) reveal.textContent = result.batch.codes.join("\n");
    } catch { toast(t("genericError"), true); }
  });
  view.querySelector("#adjust-form")?.addEventListener("submit", async event => {
    event.preventDefault();
    const data = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    try {
      await api("/hub/admin/ledger-adjustments", { method: "POST", headers: { "Idempotency-Key": crypto.randomUUID() }, body: JSON.stringify({ userId: data.get("userId"), amountUnits: Number(data.get("amountUnits")), reason: data.get("reason") }) });
      toast(t("balance"));
      await renderAdmin(view);
    } catch { toast(t("genericError"), true); }
  });
  view.querySelector("#import-form")?.addEventListener("submit", async event => {
    event.preventDefault();
    const data = new FormData(/** @type {HTMLFormElement} */ (event.currentTarget));
    const codes = String(data.get("codes") || "").split(/\r?\n/).map(value => value.trim()).filter(Boolean);
    try {
      const expiryValue = String(data.get("expiresAt") || "");
      await api("/hub/admin/recharge-batches/import", { method: "POST", body: JSON.stringify({ label: data.get("label"), unitAmount: Number(data.get("unitAmount")), codes, expiresAt: expiryValue ? new Date(expiryValue).getTime() : null }) });
      toast(t("generated"));
      await renderAdmin(view);
    } catch { toast(t("genericError"), true); }
  });
  view.querySelectorAll("[data-user-status]").forEach(button => button.addEventListener("click", async () => {
    try {
      await api("/hub/admin/user-status", { method: "PATCH", body: JSON.stringify({ userId: button.getAttribute("data-user-status"), status: button.getAttribute("data-next-status") }) });
      await renderAdmin(view);
    } catch { toast(t("genericError"), true); }
  }));
  view.querySelectorAll("[data-revoke-batch]").forEach(button => button.addEventListener("click", async () => {
    try { await api("/hub/admin/recharge-batch", { method: "DELETE", body: JSON.stringify({ batchId: button.getAttribute("data-revoke-batch") }) }); await renderAdmin(view); } catch { toast(t("genericError"), true); }
  }));
  view.querySelector("#admin-user-detail")?.addEventListener("change", async event => {
    adminSelectedUserId = /** @type {HTMLSelectElement} */ (event.currentTarget).value;
    await renderAdmin(view);
  });
  view.querySelector("#admin-batch-detail")?.addEventListener("change", async event => {
    adminSelectedBatchId = /** @type {HTMLSelectElement} */ (event.currentTarget).value;
    await renderAdmin(view);
  });
  view.querySelectorAll("[data-admin-revoke-key]").forEach(button => button.addEventListener("click", async () => {
    try { await api("/hub/admin/api-key", { method: "DELETE", body: JSON.stringify({ keyId: button.getAttribute("data-admin-revoke-key") }) }); await renderAdmin(view); } catch { toast(t("genericError"), true); }
  }));
}

async function boot() {
  try {
    const health = await api("/hub/health");
    if (Number.isSafeInteger(health.requestCostUnits) && health.requestCostUnits > 0) requestCostUnits = health.requestCostUnits;
    if (typeof health.pricingVersion === "string" && health.pricingVersion) pricingVersion = health.pricingVersion;
    if (health.proxy && Array.isArray(health.proxy.endpoints)) proxyHealth = health.proxy;
    registrationEnabled = health.registrationEnabled === true;
  } catch { /* the auth screen still explains the product when health is unavailable */ }
  try {
    const result = await api("/hub/auth/status");
    if (result.authenticated) {
      user = result.user;
      activeView = requestedView();
      renderConsole();
      await loadView();
    } else {
      renderAuth();
    }
  } catch {
    if (!user) renderAuth();
  }
}

addEventListener("hashchange", () => {
  if (!user) return;
  const nextView = requestedView();
  if (nextView === activeView) return;
  activeView = nextView;
  renderConsole();
  loadView();
});

await boot();
