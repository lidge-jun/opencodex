# hubapi 一期前端二次开发 PRD

> 文档状态：可进入设计评审与实施拆解
> PRD 版本：1.1
> 编写日期：2026-08-23
> 产品范围：公开 Landing Page + 本地 Web 控制台 + 可选托管中转站
> 产品品牌：`hubapi`
> 实施边界：基于 OpenCodex 开源仓库二次开发；托管用户平台与私有数据面强隔离

## 1. 文档目的

本文档定义 `hubapi` 一期前端二次开发的完整产品需求，作为产品、设计、前端、后端、测试和文档验收的共同依据。

一期不是对 New API 或 SimpleCard 的源码复制。目标是在保留 OpenCodex 本地优先、安全边界和现有功能的前提下，完成：

1. 可公开访问的产品 Landing Page；
2. 信息架构清晰、可观察、可解释的本地控制台；
3. 对现有管理 API 的完整、诚实呈现；
4. 桌面端与移动端一致的响应式体验；
5. 可持续跟随上游 `dev` 分支演进的前端结构；
6. 可选启用的用户登录、API 中转、整数额度账本与卡密充值平台。

托管能力是独立启用的公开准入边缘层：它负责用户认证、额度和请求结算，但不选择模型提供方、不保存 OpenCodex 管理凭据，也不替代 OpenCodex 路由器。默认本地模式的行为、启动路径和信任边界必须保持不变。

### 1.1 品牌与兼容标识

- 用户可见产品品牌与字标统一为小写 `hubapi`；
- 公开营销页、托管用户门户和 GUI chrome 不显示上游项目名，统一描述为 `hubapi` 的“私有路由核心”；
- 开源来源通过许可证、源码历史、GitHub 链接和技术参考保留，不在产品界面中作为第二品牌并列展示；
- npm 包 `@bitkyc08/opencodex`、CLI `ocx`、配置路径、服务名、HTTP Header、存储键、协议字段和上游 GitHub 链接保持不变；
- 技术页面仅在准确命令、包名、路径、Header、协议或源码链接需要时保留上游兼容标识；
- 本期不进行会破坏兼容性的底层包名或 CLI 重命名。

## 2. 基线与事实来源

### 2.1 代码基线

| 项目 | 值 |
| --- | --- |
| 上游仓库 | `https://github.com/lidge-jun/opencodex` |
| 上游集成分支 | `dev` |
| PRD 首次核对时上游提交 | `ed719b568c706f00f3fb8162b0d942d1dee358f8` |
| 最终质量门同步上游提交 | `35a89903ca8f308779b337bf50dd31c2ca2e8763` |
| 当前工作分支 | `codex/newapi-console` |
| 二次开发起始基线提交 | `9cebfc64ebc13c1743cc2ee64539d238c1cd8775` |
| 许可证 | MIT |
| GUI 技术栈 | React 19 + TypeScript + Vite + Bun |
| 文档站技术栈 | Astro + Starlight |

实施先在 `ed719b568c706f00f3fb8162b0d942d1dee358f8` 完成首次基线验证，以 `9cebfc64ebc13c1743cc2ee64539d238c1cd8775` 启动二次开发，并在最终质量门前无冲突快进至 `35a89903ca8f308779b337bf50dd31c2ca2e8763`。每个后续里程碑开始前仍必须重新核对最新 `upstream/dev`；不得以 PRD 记录的提交号代替实时事实。

### 2.2 仓库内权威来源

优先级从高到低：

1. `structure/05_gui-and-management-api.md`：GUI、认证及管理 API 边界；
2. `src/server/management/`：实际管理接口行为；
3. `gui/src/`：现有交互与数据消费方式；
4. `docs-site/src/content/docs/guides/web-dashboard.md`：面向用户的已发布行为；
5. `tests/`：接口、路由、安全和历史数据行为的回归证据。

视觉效果图仅用于表达布局和视觉方向，不是后端能力证明。图中的日期、请求量、价格、延迟、模型名称和状态均为示例数据。

### 2.3 GitHub 开源代码二次开发规则

本 PRD 的所有需求都必须作为 OpenCodex GitHub 上游代码的增量二次开发实现，不允许将仓库替换为新脚手架、复制其他控制台覆盖现有 GUI，或另建一套与 OpenCodex 配置和管理 API 平行的应用。

实施必须遵守根目录 [`AGENTS.md`](./AGENTS.md) 的“一期 PRD 驱动前端二次开发”边界，具体包括：

1. 每个里程碑开始前核对最新 `upstream/dev`，记录实际基线提交；
2. 复用现有 React、Vite、Bun、Hash 路由、管理 API、组件和数据访问模式；
3. Landing Page 在现有 Astro + Starlight 文档站内迭代；
4. 默认只修改 `gui/`、`docs-site/`、相关测试和文档；
5. 视觉需求不能自动授权修改运行时、认证、凭据、路由算法或配置格式；
6. 若现有接口无法满足需求，必须先提交源码证据、最小接口提案和前端降级方案，获得明确批准后才能扩展后端；
7. 所有改动保持为可审查、可跟随上游同步的增量 diff；
8. 代码当前行为与 PRD 不一致时，先交叉验证并修订需求，不得伪造数据使画面“看起来完成”。

## 3. 产品定义

### 3.1 一句话定位

hubapi 是一个以内置私有路由核心为基础的多模型 AI API SaaS 网关：本地模式让操作者通过一个入口管理多个模型提供方；可选托管模式增加用户登录、公开 API 准入、额度账本和卡密充值，模型选择与协议适配仍由同一私有路由核心完成。

产品的核心不是把所有请求固定转发到同一个模型，而是把可审计的“任务路由”暴露为稳定模型别名。客户端在 `model` 字段声明任务路由（例如 coding、vision、fast 或 private）；私有路由核心根据该路由配置、请求可直接验证的能力要求、候选健康、延迟、成本和额度证据选择最终提供方与模型，并记录决策轨迹。第一期不读取或持久化提示词来猜测任务，不增加不可解释的语义分类器。

### 3.2 目标用户

#### A. 个人开发者

- 在本机使用多个 AI 模型提供方；
- 希望快速连接 Codex、Claude Code 或其他客户端；
- 需要理解一次请求最终走向哪个模型和提供方；
- 不希望把密钥和管理数据交给第三方 SaaS。

#### B. 小型技术团队的网关操作者

- 维护多个 API Key、OAuth 账户或本地模型端点；
- 需要观察健康、配额、失败、延迟和 Token 使用；
- 需要配置路由、组合、回退和子代理模型；
- 需要在出现 401、429、5xx 或模型不兼容时快速定位问题。

#### C. 托管中转站用户

- 注册并登录 hubapi 用户门户；
- 创建、查看前缀和撤销自己的公开 API Key；
- 使用卡密充值整数额度并查看不可篡改的账本记录；
- 通过 OpenAI/Anthropic 兼容端点使用管理员已配置的模型；
- 只查看自己的请求用量、扣费结果和错误，不接触提供方凭据或管理控制台。

### 3.3 核心用户任务

1. 第一次进入时，完成一个提供方和一个模型的最短可用配置；
2. 在 30 秒内判断代理是否在线、请求是否健康、当前默认提供方和模型是什么；
3. 从请求日志解释“为什么这次请求走到这里”；
4. 在不暴露密钥和提示词的前提下完成故障诊断；
5. 为外部客户端生成和撤销数据面 API Key；
6. 管理 Codex/ChatGPT 账户池、提供方账户池和故障回退；
7. 在桌面和移动设备上完成查看与低风险管理操作。
8. 用户登录后完成卡密充值，创建公开 API Key，并用同一 Base URL 发起受额度约束的流式请求。
9. 为 coding、vision、fast、private 等真实业务任务配置稳定路由别名，并让客户端用同一 API 按任务选择模型。

## 4. 产品原则

### 4.1 波兰尼原则：围绕操作者真实工作流设计

操作者的隐性知识不是“知道每个 API 名称”，而是知道：

- 哪个客户端发起请求；
- 请求模型是什么；
- 命中了哪条路由规则；
- 尝试过哪些提供方；
- 失败发生在哪一步；
- 最终模型和结果是什么。

因此控制台以“请求路径”和“问题处理”组织信息，而不是按后端文件或配置字段机械分组。

### 4.2 剃刀原则：优先重组，不重写

- 保留 React、Vite、Bun、Hash 路由、现有 API 客户端和状态模式；
- 不为了模仿 New API 引入新的 Router、状态管理、CSS 框架或图表框架；
- 现有接口能满足需求时不得新增平行接口；
- 现有组件能扩展时不得建立第二套组件系统；
- 新依赖必须有无法用现有栈合理完成的证据，并接受显式安全审查。

### 4.3 证据优先

- 未测量数据展示 `N/A`，不得转换为 `0`；
- 估算价格明确标记“列表价估算，不是账单”；
- 部分覆盖率不得表达为完整统计；
- 老日志没有路由轨迹时显示诚实空状态；
- UI 不根据视觉稿虚构服务状态、模型能力或配额。

### 4.4 本地优先与隐私

- 不增加第三方行为分析、埋点或远程字体依赖；
- 不展示或记录请求正文、提示词、完整密钥、邮箱或可识别账户信息；
- 管理面和数据面凭据始终分离；
- 本地环回和非环回访问沿用现有认证策略。
- 托管用户会话、公开 API Key、内部准入密钥和本地管理凭据四者相互隔离；
- 托管模式显式启用，普通本地模式不得加载其代码、数据库、监听器或定时器。

## 5. 一期目标与成功标准

### 5.1 目标

| ID | 目标 |
| --- | --- |
| G1 | 公开首页能准确解释 hubapi 是什么、如何工作、如何开始，并统一呈现私有路由核心 |
| G2 | 控制台一级导航与用户任务一致，核心页面两次点击内可达 |
| G3 | 总览能同时呈现健康、用量、提供方和待处理问题 |
| G4 | 日志能解释真实路由决策、尝试序列和最终结果 |
| G5 | 提供方、模型、API Key、账户池、路由、集成及存储现有功能无回退 |
| G6 | 390px 至 1440px 视口均可操作，达到 WCAG 2.2 AA 关键要求 |
| G7 | 所有新增文案覆盖仓库现有全部 GUI 语言 |
| G8 | 托管用户可安全登录、充值卡密、管理自己的 API Key 并调用中转 API |
| G9 | 公开用户面与 OpenCodex 私有管理面之间没有凭据、路由或数据越权 |

### 5.2 可验证成功标准

- 新用户可从 Landing Page 找到安装、快速开始、提供方、路由、仪表板和管理 API 文档；
- 配置过提供方的用户可在总览看到真实在线状态和最近数据，而不是静态示例；
- 用户可从一条日志进入真实路由决策详情；
- 老日志、未测量 Token、无价格和索引修复状态均有明确表达；
- 所有原有 Hash 深链接仍能打开正确目标；
- GUI 全量测试、lint、i18n lint、build 通过；
- 若实现过程中修改 `src/`，根目录 typecheck、完整测试和 privacy scan 通过。
- 卡密并发兑换只能成功一次；相同请求幂等重试只能结算一次；余额不足在进入 OpenCodex 前拒绝；
- 本地默认启动不创建 hub 数据库、不绑定公开端口，也不导入托管模块；
- 公开用户凭据无法访问任何 `/api/*` 管理端点，管理会话也不被当作公开 API Key；
- 会话与密钥不进入 Local Storage、URL、普通日志、错误正文或静态构建产物。

## 6. 一期范围

### 6.1 In Scope

1. `docs-site` Landing Page 视觉与内容重构；
2. GUI 全局设计系统和应用壳重构；
3. 总览；
4. 提供方与账户池；
5. 模型目录、自定义模型、组合、路由策略和兼容性；
6. API Key 管理；
7. 用量分析；
8. 请求日志、请求详情和路由决策；
9. 子代理；
10. Codex Auth；
11. 客户端集成；
12. 启动安全、更新、调试、存储、清理、恢复和停止；
13. 明暗主题、响应式、键盘操作、空态、错误态和加载态；
14. 文档同步、测试与视觉回归。
15. 可选托管用户门户：注册、登录、退出、会话管理和个人资料；
16. 用户公开 API Key：创建后一次显示、列表掩码、撤销；
17. 整数额度余额、追加式账本、卡密生成/导入/作废/兑换；
18. 管理员用户、卡密批次、额度调整与审计视图；
19. 公开准入边缘层：认证、额度预留/结算、幂等、流式转发和取消传播；
20. SimpleCard 代码库许可证、依赖、认证、授权、卡密、支付和部署安全审计。

### 6.2 Out of Scope

- 组织、团队与可配置 RBAC（一期仅 `user` / `admin` 固定角色）；
- 钱包、支付通道、订单、发票与自动结算到法币或链上资产；
- 订阅套餐、优惠券、分销和自动续费；
- API Key 到期时间、RPM 限制、来源 IP/Origin 限制、按 Key 限定模型；
- 将本地控制台嵌入 iframe；
- 提示词或响应正文查看器；
- 代理运行时、适配器协议和路由算法重写；
- 自动执行 GitHub Star 或任何使用用户身份的外部操作；
- 像素级复制 New API 或复用其 AGPL 源码。

上述 API Key 限制项曾出现在概念视觉稿中，但当前 OpenCodex 接口只支持创建、列出、重命名、撤销和用量归属。本期实现必须按真实能力修正该画面。

托管用户 API Key 是新的公开边缘层能力，不等同于 OpenCodex 本地数据面 Key。两类 Key 必须使用不同前缀、存储、认证器和页面；边缘层只向私有 OpenCodex 转发内部准入凭据。

## 7. 信息架构

### 7.1 公开站点

```text
Landing
├── 产品能力
├── 工作原理
├── 集成
├── 文档
├── GitHub
└── 开始使用
```

### 7.2 托管用户门户

```text
登录 / 注册
├── 用户总览（余额、近期用量、状态）
├── API 密钥（创建、一次显示、撤销）
├── 卡密充值（兑换、结果、幂等重试）
├── 账本（充值、预留、结算、退款、调整）
└── 账户安全（会话、密码、退出全部设备）
```

### 7.3 本地及托管管理员控制台导航

```text
总览

资源
├── 提供方
├── 模型
└── API 密钥

观测
├── 用量
└── 请求日志

自动化
├── 路由策略
└── 子代理

系统
├── 客户端集成
└── 运行与存储
```

术语统一使用“提供方”，与现有 `zh.ts` 的 `nav.providers` 保持一致。效果图中的“供应商”应在实现稿中改为“提供方”。

### 7.4 新旧路由映射

| 新入口 | 保留/目标 Hash | 说明 |
| --- | --- | --- |
| 总览 | `#dashboard` | 保留 |
| 提供方 | `#providers` | 保留 |
| Codex Auth | `#codex-auth` | 页面保留，可从提供方工作区进入 |
| 模型目录 | `#models` | 保留 |
| 路由策略 | `#models/routing` | 保留 Models 子路由 |
| 组合 | `#models/combos` | 保留 Models 子路由 |
| 兼容性 | `#models/compatibility` | 保留 Models 子路由 |
| API 密钥 | `#integrations/keys` | 提升为一级入口，不复制页面 |
| 用量 | `#usage` | 保留 |
| 请求日志 | `#logs` | 保留 |
| 调试 | `#logs/debug` | 保留 Logs 子路由 |
| 子代理 | `#subagents` | 保留 |
| 客户端集成 | `#integrations` | 保留 |
| 启动安全 | `#startup` | 运行与存储工作区入口之一 |
| 存储 | `#storage` | 运行与存储工作区入口之一 |

旧 `#api`、`#claude`、`#grok`、`#combos`、`#routing`、`#lab` 和 `#providers/workspace` 的兼容重定向不得删除。

## 8. 视觉设计系统

### 8.1 设计主题

主题名称：**Routing Evidence Console**。

视觉记忆点不是卡片或渐变，而是“路由轨迹”：将客户端、请求模型、策略、提供方、最终模型、尝试和结果组织为可读证据链。

### 8.2 色彩

| 角色 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| 页面背景 | `#F6F8FB` | `#17191D` | 应用背景 |
| 内容表面 | `#FFFFFF` | `#23262B` | 卡片、表格、抽屉 |
| 主文字 | `#18202B` | `#ECEFF3` | 标题、正文 |
| 次文字 | `#667085` | `#A7AFBA` | 描述、辅助数据 |
| 操作蓝 | `#3157D5` | `#7C9BFF` | 主按钮、选择、焦点 |
| 路由青 | `#159A9C` | `#50C7C8` | 请求路径和实时流动 |
| 成功绿 | `#0A7D5C` | `#4ECB9D` | 在线、成功 |
| 告警黄 | `#B86A08` | `#FBBF24` | 降级、配额、重试 |
| 错误红 | `#C43D4B` | `#F87171` | 失败和破坏性操作 |

颜色不能成为唯一状态载体；状态必须同时使用图标、文字或形状。

### 8.3 字体

- GUI：沿用现有本地系统字体栈和 Pretendard 回退；
- Landing：复用文档站已有 Geist Variable 与 Pretendard；
- 技术数据：沿用现有等宽字体栈；
- 一期不新增字体依赖。

模型 ID、请求 ID、端点、环境变量和命令必须使用等宽字体。普通标签不得滥用等宽字体。

### 8.4 栅格与尺寸

- 基础间距单位：4px；
- 桌面侧栏：224–232px；
- 主内容最大宽度：1600px，宽屏居中；
- 内容卡片圆角：8–12px；
- 触摸目标最小：44px；
- 正文最小：14px；
- 表格辅助文字不得小于 12px；
- 表格数字使用 tabular nums。

### 8.5 核心组件

1. App Shell：桌面侧栏、移动顶部栏、抽屉和内容区；
2. Page Header：标题、说明、主操作和过滤器；
3. Route Trace：水平、垂直和紧凑三种变体；
4. Status Badge：成功、警告、失败、禁用、未知；
5. Data Table：排序、过滤、分页、选中详情；
6. Detail Inspector：桌面右侧面板，移动端全屏抽屉；
7. Metric Card：有数据、部分覆盖、N/A 三种状态；
8. Quota Bar：当前值、阈值、恢复时间、数据新鲜度；
9. Empty State：解释为什么为空并提供下一步；
10. Error State：说明失败对象、保留状态和重试操作；
11. Confirm Dialog：明确对象、后果和可恢复性；
12. Copy Field：隐藏敏感内容并提供反馈；
13. Data Freshness：实时、缓存、陈旧、刷新失败。

## 9. 页面需求

## 9.1 Landing Page

### 目标

让首次访问者在一个首屏内理解 hubapi 的对象、价值、工作方式和开始入口。

### 页面结构

1. 顶部导航：产品能力、工作原理、集成、文档、GitHub；
2. Hero：标题“一个入口，连接你的全部 AI 模型”；
3. 工作原理拓扑：客户端 → hubapi 私有路由核心 → 多个提供方；
4. 已支持提供方与客户端，仅展示代码或权威文档确认的对象；
5. 三个任务块：连接提供方、控制模型与路由、观察请求；
6. 请求日志和路由解释示例；
7. 本地优先与凭据边界；
8. 安装命令及快速开始；
9. 文档导航和页脚。

### 内容约束

- 不显示无法证实的客户 Logo、性能倍数、节省金额或市场排名；
- “40+ 提供方”等数量必须从当前仓库事实核对后输出，不能硬编码为永久承诺；
- 安装命令必须来自当前 README/安装文档；
- GitHub Star 只能由真实用户主动触发；
- Landing 不读取本地管理 API，不显示用户本机运行状态；
- 所有现有 Landing 语言保持可用，英文为内容源，翻译不得矛盾。

### 验收

- 320px、390px、768px、1440px 无横向溢出；
- 关闭 JavaScript 时核心文案和链接可读；
- 深色主题信息层级完整；
- 无外部遥测和远程字体请求；
- 文档站构建通过。

## 9.2 总览

### 目标

在 30 秒内回答：服务是否正常、近期请求是否健康、提供方是否可用、有什么需要处理。

### 内容

1. 服务状态：在线、版本、运行时间、启动安全；
2. 当前默认提供方、默认模型、可用提供方/模型数量；
3. 近期请求、Token、成功率、延迟；
4. 请求与 Token 趋势；
5. 提供方健康摘要；
6. 最近路由轨迹；
7. 待处理问题：认证、配额、模型同步、更新和存储；
8. 快捷操作：同步模型、添加提供方、查看异常日志。

### 数据规则

- 指标必须来自现有状态、用量、路由分析或请求历史；
- 指标窗口必须可见，例如 24h、7d、30d；
- 没有足够数据时显示 `N/A` 和原因；
- 成功率必须显示其覆盖范围，不把无状态记录计作成功；
- 待处理问题只来源于可验证状态，不使用启发式文案制造告警。

### 交互

- 点击提供方健康行进入提供方详情；
- 点击异常请求进入日志详情；
- 点击路由轨迹节点进入对应模型、策略或提供方；
- 同步和更新操作显示进行中、成功、降级成功和失败四种结果。

## 9.3 提供方

### 目标

连接和维护模型服务、OAuth 账户、API Key 池、默认提供方及健康状态。

### 列表字段

- 名称和提供方类型；
- 连接方式；
- 启用/默认状态；
- 模型发现状态和数量；
- 健康、配额或冷却状态；
- 最近检查时间；
- 操作菜单。

### 详情区

1. 连接设置；
2. 模型发现开关；
3. OAuth 账户池或 API Key 池；
4. 活跃账户/Key 和别名；
5. 配额、速率限制和冷却；
6. 上下文上限；
7. 测试连接；
8. 设置默认、启停和删除。

### 行为约束

- 真实凭据不得回填；掩码值不得作为 PUT/POST 密钥重新提交；
- 删除当前默认提供方时沿用服务端的重新分配或拒绝规则；
- 最后一个可用提供方不能在服务端拒绝时被 UI 假装删除；
- 提供方测试必须展示测试的模型、耗时和错误，不只显示红/绿；
- OAuth 风险和提供方条款警告保留；
- 账户邮箱不得出现在日志或全局健康卡中。

## 9.4 模型与路由

### 目标

管理客户端可见模型、自定义模型、上下文上限、组合、路由策略和兼容性。

### 子页面

#### 模型目录

- 按原生/路由和提供方分组；
- 搜索模型 ID 和显示名；
- 显示文本、视觉等服务端确认的能力；
- 切换最终可见性；
- 管理提供方 allowlist；
- 管理上下文上限；
- 添加、编辑和删除自定义模型；
- 显示模型发现关闭、失败或返回空列表的原因。

#### 组合

- 查看和编辑候选模型顺序；
- 明确顺序、失败条件和最终回退；
- 不把组合描述成并行请求，除非实际配置如此；
- 保存前验证模型和提供方存在。

#### 路由策略

- 列表、创建、编辑、删除；
- 将策略别名作为客户端可请求的任务路由；任务名称是显式契约，不从提示词正文推断；
- 条件、候选、优先级和修订号；
- 根据可验证的工具、图像、结构化输出、上下文、本地性等能力要求过滤候选，再按健康、延迟、成本和额度权重排序；
- Dry Run 输入与匹配结果；
- 路由分析：可靠性、延迟、估算成本和覆盖范围；
- 策略变化不追溯修改历史请求。

#### 兼容性

- 展示真实探测结果和时间；
- Lab 保持可选，不在普通用户路径启动；
- 无证据时使用未知，不推断兼容。

### 标志性交互

只读“路由预览”按证据来源分开：配置预览复用“路由策略”页的 Dry Run，历史真实轨迹由“请求日志”详情中的 route-decision 提供。模型目录只提供入口和口径说明，不复制一套前端路由推断器。两种证据必须明确标注，不能混用。

## 9.5 代理 API 与密钥

### 目标

把 OpenCodex 已有数据面明确呈现为 hubapi 统一 AI API 代理网关，并创建、识别、重命名、观察和撤销数据面 API Key。这里不新增第二个代理运行时，也不改变既有端点契约。

### 现有能力内需求

- 创建时填写名称；
- 创建后仅一次显示完整 `ocx_data_` 密钥；
- 列表只显示安全前缀；
- 展示创建时间；
- 展示可归属的 7 日和总请求量；
- 显示 `attributionSince` 和历史截断提示；
- 重命名；
- 撤销；
- 展示 Base URL、Responses、Chat Completions、Messages 和 Models 端点；
- 在一级入口展示代理在线状态、`客户端 → hubapi → 提供方` 流向、Base URL、可用入站协议和真实认证请求头；
- 展示服务端返回的认证矩阵；
- 模型端点测试与 Key 管理保持独立加载，不互相阻塞。

### 明确不实现

- 暂停/恢复 Key；
- 到期时间；
- 按 Key 限定模型；
- RPM/TPM 限制；
- 来源 IP、域名或 Origin 限制；
- 管理 API 权限复选框。

### 安全交互

- 新密钥弹窗关闭前要求用户确认已经复制；
- UI 不把密钥写入 URL、本地存储、日志或分析；
- 撤销必须显示 Key 名称和不可恢复结果；
- 重命名响应不得包含密钥材料。

## 9.6 用量

### 目标

解释近期请求和 Token 使用，而不是伪装成计费账单。

### 内容

- 时间范围和 surface 过滤；
- 每日请求与 Token 趋势；
- 输入、输出、缓存和推理 Token（仅在可测量时）；
- 按模型、提供方、surface 的分布；
- measured/reported/unreported/unsupported/estimated 覆盖说明；
- 列表价估算及价格来源；
- 数据为空、部分读取和历史截断状态。

### 规则

- 成本始终标注为 API 列表价等价估算；
- 订阅、积分和提供方优惠不推断为真实账单；
- 历史价格重新估算时展示当前配置口径；
- 不把无法读取的 Token 补零。

## 9.7 请求日志与路由决策

### 目标

让操作者基于持久化请求元数据定位错误并解释路由结果。

### 列表

- 使用 `/api/request-history` 作为可分页历史主接口；
- 支持提供方、模型、请求模型、状态、会话、surface、协议、API Key ID、策略、是否 fallback、时间范围过滤；
- 使用服务端 cursor，不在前端伪造页码总量；
- 字段包括时间、surface/客户端、请求模型、最终模型、提供方、Token、耗时、状态和请求 ID；
- 索引状态异常或修复时显示数据新鲜度信息。

### 详情

- 基础请求元数据；
- Token 与用量状态；
- 请求与最终模型；
- 最终提供方；
- 状态、错误码、终止状态和关闭原因；
- 请求耗时；
- opaque conversation/session id；
- 请求 ID 复制；
- 同一会话过滤。

### 路由决策

通过 `/api/request-history/:requestId/route-decision` 展示：

1. 请求模型；
2. 路由类型；
3. 命中的策略与修订号；
4. 选中的提供方和模型；
5. `attemptSequence`；
6. fallback 或重试；
7. 最终提供方、模型与结果。

老记录没有 `routeDecision` 时显示：“此请求早于路由轨迹记录，无法解释具体决策。”不得根据最终提供方反推完整路径。

### 隐私

- 不展示提示词、响应内容、完整 Key、账户邮箱；
- 对话 ID、Key ID 和账户标签按现有隐私规则处理；
- Debug 与普通请求日志保持分区，避免把诊断噪声混入主要工作流。

## 9.8 子代理

### 目标

配置可出现在 `spawn_agent` 覆盖列表中的模型，并准确解释这些设置的作用范围。

### 需求

- 最多五个推荐模型；
- 原生和路由模型均可选择；
- 显示支持的推理强度；
- 配置 v1/base/v2 surface 和线程限制；
- 配置注入模型、effort、guidance 和 native defaults；
- 明确这不是代理侧跨模型 spawn router；
- 当前会话和新会话的生效范围必须写清楚。

## 9.9 Codex Auth

### 目标

管理 ChatGPT/Codex 主账户和账户池，显示配额、顺序、固定账户及自动切换状态。

### 需求

- Pool/Direct 模式；
- 添加和移除账户；
- 刷新 5h、weekly、30d 窗口；
- 账户选择顺序；
- PINNED 状态；
- quota auto-switch、阈值和 transient failure failover；
- picker 中针对指定账户的显式 opt-in；
- 匿名日志标签、覆盖率和列表价估算；
- 重新认证、冷却和数据陈旧状态。

### 合规文案

账户池是技术路由与恢复能力，不得宣传为规避提供方配额或条款的手段。

## 9.10 客户端集成

### 目标

在一个工作区管理 API、Codex、Claude Code、Claude Desktop、Grok、OpenCode、Pi、OMP、Hermes、OpenClaw、Kimi、Gajae、DSH、MCode、ZCode、Prime 等当前代码支持的客户端。

### 需求

- 外层客户端标签保持可深链接；
- 只展示代码当前注册的集成；
- 显示连接状态、受管配置范围、预览和应用结果；
- 修改前显示会改哪些文件/字段、不会改哪些内容；
- 支持 journal 和 restore 的集成必须提供恢复入口；
- API Key 页面从本工作区提升一级入口，但仍复用同一组件和数据源。

## 9.11 运行与存储

### 启动安全

- 代理、后台服务、launcher shim 和重启安全状态；
- 固定 allowlist 操作；
- Windows tray 安装、启动、停止和卸载；
- Codex autostart 只描述其真实作用，不暗示它会安装服务；
- 项目局部配置绕过警告；
- 更新检查、运行和状态恢复。

### 存储

- CODEX_HOME 分类和大小；
- 活跃会话只读；
- 清理预览；
- 默认移入 `.trash`；
- 永久删除必须显式勾选；
- 自动清理默认关闭；
- 策略阈值、目标、计划和模式；
- Trash 列表和恢复；
- SQLite 锁存在时按服务端拒绝展示原因；
- Codex 日志保护、修复、压缩等现有操作保持可达。

### 停止代理

- 破坏性样式；
- 明确会恢复原生 Codex、停止已安装服务并退出代理；
- 服务端拒绝或恢复失败时不得显示为成功。

## 9.12 托管用户、中转与卡密

### 架构

- `hub` 是显式启动的公开准入边缘层，不是第二个模型路由器；
- 公开端口只暴露用户门户、用户 API 和兼容数据面，不暴露 OpenCodex `/api/*` 管理面；
- OpenCodex 监听私有 loopback 地址，边缘层验证用户 Key 和额度后，用独立内部准入密钥进行流式转发；
- 默认 `ocx start` 不导入 `hub` 模块、不创建 hub 数据库、不启动监听器或定时任务；
- 一期采用单节点 SQLite 和进程独占锁；多副本部署必须等待 PostgreSQL 事务适配器，不得用共享 SQLite 冒充集群能力。

### 用户与会话

- 支持注册、登录、退出、退出所有设备、修改密码和当前会话列表；
- 用户名/邮箱规范化后唯一；错误响应不得泄漏账户是否存在；
- 密码使用 `Bun.password` Argon2id，配置最小长度和常见密码拒绝；
- 登录成功轮换会话；会话使用服务端随机不透明 Token，数据库只存摘要；
- 浏览器只接收 `Secure; HttpOnly; SameSite=Lax` Cookie，生产环境不允许降级到 Local Storage Bearer Token；
- 写操作同时校验 Origin 和 CSRF，登录/注册/重置/卡密兑换按账户和可信客户端 IP 限速；
- 一期固定 `user`、`admin` 两个角色，不提供用户自定义权限。

### 用户 API Key

- 用户可以创建、命名、查看掩码、查看最后使用时间和撤销自己的 Key；
- Key 采用 `hub_live_` 前缀和 CSPRNG 随机材料，完整值只在创建响应显示一次；
- 数据库保存 HMAC 摘要、前缀、末四位、用户 ID、状态和时间，不保存完整值；
- 用户 Key 只能调用公开数据面，不能访问用户管理 API、管理员 API 或 OpenCodex 管理面；
- 管理员可以停用用户或撤销 Key，但不能查看完整 Key。

### 额度、账本与卡密

- 额度使用整数最小单位；价格换算规则版本化，严禁用浮点数直接改余额；
- 账本只追加，至少包含充值、预留、结算、释放/退款和人工调整类型；
- 每笔变更包含唯一幂等键、主体、数量、原因、关联请求和时间；
- 卡密批次支持生成或导入、面额、数量、过期时间、停用和导出一次；
- 卡密使用独立 HMAC 域摘要存储，列表只显示批次、末四位、状态和兑换者，不保存可恢复明文；
- 兑换在单个数据库事务内校验状态、写入兑换记录和账本，唯一约束保证并发只能成功一次；
- 管理员调整必须填写原因并进入不可变审计记录；不允许直接编辑余额字段。

### 中转请求生命周期

1. 解析并摘要查找公开 API Key；
2. 检查用户、Key、模型可用范围和额度；
3. 以客户端幂等键或服务端请求 ID 原子创建额度预留；
4. 删除公开凭据，注入内部准入凭据，流式转发到私有 OpenCodex；
5. 传播断开和取消；请求正文只在受 8 MiB 上限约束的临时内存中用于指纹与转发，不写日志或数据库，响应保持流式且不在边缘层整段缓冲；
6. 一期采用明确、版本化的固定整数计费：私有 OpenCodex 返回 2xx 即视为接受并结算；接受前失败释放预留，接受后取消、流错误或进程恢复按保守策略结算并记录原因；不得把缺失的 Token 证据伪造成零成本；
7. 重试、断线恢复和重复回调均不能重复扣费；每次释放或保守结算都写入终态原因。

### 管理与审计

- 管理员可查看用户状态、掩码 Key、余额、账本、卡密批次和聚合用量；
- 敏感操作要求近期重新认证，使用固定动作 allowlist，并记录操作者、对象、动作和结果；
- 日志不记录邮箱、完整 Key、卡密、Cookie、内部准入密钥、请求正文、响应正文或支付签名；
- 一期不接入易支付、USDT、钱包签名或自动订单交付。任何支付扩展必须另行安全评审和人工批准。

### SimpleCard 二次开发结论

- 参考仓库：`runtimepoet/simplecard`，审计基线提交 `2f9b68e30d9c8b0d1a4917b0d2042003477c92ab`；
- 其 MIT 许可证允许参考和再实现，但 Next.js/Spring Boot 运行时不直接并入本仓库；
- 采用其商品/卡密/用户流程作为领域研究输入，不继承默认账户、Token 存储、依赖树、支付通道或部署默认值；
- 详细未修复安全发现只保存在 `.tmp` scratch，不写入公开 `devlog` 或文档站。

## 10. 数据与接口映射

| 领域 | 主要现有接口 | 一期用途 |
| --- | --- | --- |
| 设置/状态 | `/api/config`, `/api/settings` | 总览、设置与生效状态 |
| 启动 | `/api/startup-health`, `/api/startup-action`, `/api/windows-tray` | 运行安全 |
| 更新 | `/api/update/check`, `/api/update/run`, `/api/update/status`, `/api/update/badge` | 更新提示与进度 |
| 提供方 | `/api/providers`, `/api/providers/test`, `/api/provider-presets` | CRUD、默认、启停、测试 |
| 提供方额度 | `/api/provider-quotas`, `/api/provider-request-pacing` | 配额、冷却和请求节奏 |
| OAuth | `/api/oauth/providers`, `/api/oauth/login`, `/api/oauth/status`, `/api/oauth/accounts` | 登录和账户池 |
| 提供方 Key 池 | `/api/providers/keys` 及其 active/alias 操作 | Key 池管理 |
| 模型 | `/api/models`, `/api/catalog`, `/api/model-visibility`, `/api/selected-models` | 模型目录和可见性 |
| 自定义模型 | `/api/custom-models` | 自定义模型 CRUD |
| 上下文 | `/api/provider-context-caps` | 上下文上限 |
| 组合 | `/api/combos` | 组合管理 |
| 路由 | `/api/routing-profiles`, `/api/routing-profiles/dry-run` | 路由策略和预演 |
| 路由分析 | `/api/routing-analytics` | 可靠性、延迟和成本分析 |
| API Key | `/api/keys` | 创建、列表、重命名、撤销、用量 |
| 历史请求 | `/api/request-history` | 游标分页和过滤 |
| 路由解释 | `/api/request-history/:id/route-decision` | 真实轨迹、尝试和结果 |
| 兼容日志 | `/api/logs` | 现有最近日志兼容面 |
| 用量 | `/api/usage` | 趋势、覆盖率和估算 |
| 调试 | `/api/debug` 及 debug 子路由 | 可选诊断 |
| 子代理 | `/api/subagent-models`, `/api/injection-model`, `/api/v2` | 子代理和 surface |
| Codex Auth | `/api/codex-auth/*` | 账户池、配额和切换 |
| 集成 | `/api/client-integrations`, journal/restore 子路由 | 客户端配置 |
| 原生集成 | `/api/native-integrations` | Codex/Claude/Grok 等开关 |
| 存储 | `/api/storage` 及 cleanup/trash/policy 子路由 | 空间、清理和恢复 |
| 系统 | `/api/system/memory`, `/api/system/restart`, `/api/stop` | 内存、重启和停止 |
| 托管认证 | `/hub/auth/register`, `/hub/auth/login`, `/hub/auth/logout`, `/hub/auth/sessions` | 公开用户会话；与管理 API 完全隔离 |
| 用户 Key | `/hub/account/keys`, `/hub/account/key` | 一次显示、掩码列表、固定 URL 撤销 |
| 余额、账本、请求与模型 | `/hub/account/balance`, `/hub/account/ledger`, `/hub/account/requests`, `/hub/account/models` | 整数余额、追加式记录、本人请求终态和受限模型目录 |
| 卡密兑换 | `/hub/account/redeem` | 原子一次兑换与幂等返回 |
| 托管管理 | `/hub/admin/users`, `/hub/admin/user-details`, `/hub/admin/api-key`, `/hub/admin/metrics`, `/hub/admin/requests`, `/hub/admin/recharge-batches`, `/hub/admin/recharge-batch`, `/hub/admin/recharge-code-inventory`, `/hub/admin/ledger-adjustments`, `/hub/admin/audit` | 固定 admin 角色管理、固定动作 URL 与脱敏支持活动 |
| 公开中转 | `/v1/responses`, `/v1/chat/completions`, `/v1/messages`, `/v1/models`（独立公开监听器） | 用户准入、额度预留/结算、流式转发 |

前端不得依赖未列入实现代码的字段。实施时应为每个页面建立请求/响应类型验证，不因字段缺失自动补出权威零值。

## 11. 全局交互要求

### 11.1 加载

- 首次加载使用与真实布局一致的 skeleton；
- 有缓存数据时保留数据并显示后台刷新；
- 独立资源独立加载，模型目录慢不能阻塞 API Key 管理；
- 超时后恢复操作能力并提供重试。

### 11.2 空状态

| 场景 | 行为 |
| --- | --- |
| 无提供方 | 引导添加第一个提供方 |
| 无模型 | 说明发现关闭、失败或返回空，提供相应入口 |
| 无 API Key | 引导创建，不暗示已有默认 Key |
| 无日志 | 说明尚无请求或过滤条件无匹配 |
| 无路由轨迹 | 区分老数据和确实没有策略 |
| 无用量 | 区分未产生请求、未测量和读取失败 |

### 11.3 错误与降级成功

- 错误说明对象、发生动作和可执行修复；
- 保存成功但目录同步失败时使用告警成功，不显示纯成功；
- 删除、恢复、更新和停止按服务端最终结果反馈；
- 网络失败不得乐观更新永久状态；
- 401、403、409、429 和 5xx 使用不同文案。

### 11.4 动效

- Route Trace 只在真实进行中状态显示流动；
- 页面切换使用 120–180ms 轻量过渡；
- 不使用持续背景动画；
- `prefers-reduced-motion` 下取消非必要动画；
- 动画不得延迟控制响应或掩盖状态变化。

## 12. 响应式要求

| 视口 | 布局 |
| --- | --- |
| ≥ 1280px | 固定侧栏，内容区，必要时右侧详情面板 |
| 1024–1279px | 固定/可折叠侧栏，详情面板覆盖或缩窄 |
| 768–1023px | 抽屉导航，双列卡片，表格允许横向滚动 |
| < 768px | 顶部栏、全屏抽屉、单列卡片、关键底部导航 |

移动端要求：

- 不把桌面侧栏缩成窄列；
- 数据表优先转为卡片或保留首要字段；
- 详情使用全屏 sheet；
- 破坏性操作不放入容易误触的底部主导航；
- Route Trace 支持横向滚动并有可见的滚动提示；
- 编辑复杂路由策略时允许提示使用宽屏，但查看和低风险操作仍可完成。

## 13. 国际化与文案

- GUI 新增可见字符串必须进入 `en.ts` 和全部 locale 文件；
- 英文为 key/source truth，各翻译不得改变产品边界；
- 产品名、模型 ID、协议、端点和命令不翻译；
- 中文统一使用“提供方”“请求日志”“路由策略”“API 密钥”；
- 操作按钮使用明确动词，如“保存更改”“撤销密钥”“测试连接”；
- 不使用“提交”“处理失败”“发生错误”等无对象的模糊文案；
- 技术错误可折叠展示，首层文案必须可执行。

## 14. 可访问性

- 目标：WCAG 2.2 AA；
- 所有功能可通过键盘完成；
- 焦点顺序与视觉顺序一致；
- Drawer/Modal 有焦点锁定、返回焦点和 Escape 关闭；
- 图表提供摘要、图例和可访问数据表；
- 状态不只依赖颜色；
- 文本与背景对比度达到 AA；
- 表单有显式 label、描述和错误关联；
- loading、保存结果和错误通过适当 live region 通知；
- 44px 触摸目标覆盖移动端核心操作。

## 15. 安全与隐私要求

1. OpenCodex 数据面 Key、管理面 Token、GUI Session、hub 用户会话、hub 用户 Key 和内部准入密钥全部保持互斥；
2. Loopback GUI Session 保持同源和短时效；
3. 非 Loopback 管理访问继续要求管理凭据和 Origin 策略；
4. 管理凭据不写入 localStorage/sessionStorage；
5. 数据面 API Key 仅创建时返回完整值；
6. UI 不记录请求正文、提示词、完整 Key、邮箱或账户标识；
7. 原生写操作继续要求现有 CSRF/GUI Session 证据；
8. 不移除 `X-Frame-Options: DENY` 和 `frame-ancestors 'none'`；
9. GitHub Star 等身份行为必须由用户真实点击；
10. 任何认证、OAuth、Token、依赖或工作流变更必须显式安全评审；
11. `privacy:scan` 必须通过；
12. 前端错误信息不得序列化完整配置或凭据。
13. hub 浏览器会话只使用服务端不透明 Session Cookie，禁止 Local Storage Bearer Token；
14. 密码使用 Argon2id；会话、用户 Key 和卡密仅存摘要，完整材料只显示一次；
15. 无默认管理员、默认密码、占位密钥或自动回退密钥；不安全配置必须阻止托管模式启动；
16. 所有用户资源做服务端主体归属校验；对象 ID、请求 ID、URL 和前端隐藏都不是授权；
17. 账本、兑换、预留和结算必须事务化、幂等且通过并发回归测试；
18. 认证、授权、密钥、额度和审计变更必须完成显式网络安全评审。

## 16. 性能要求

- Landing 首屏避免加载 Dashboard GIF 等大资源作为阻塞内容；
- 非当前工作区不持续轮询；
- 大模型列表和日志列表使用现有虚拟化能力；
- 请求历史使用服务端 cursor 分页；
- 图表按可见时间窗读取，避免一次加载完整 usage.jsonl；
- 页面切换不重复获取未过期且仍可信的资源；
- 缓存必须保留数据来源与时间，不得把陈旧缓存伪装为实时；
- 目标不是硬编码某个实验室分数，最终以实际浏览器性能测量和无明显交互阻塞为验收依据。

## 17. 兼容与迁移

- 现有 Hash URL 和浏览器前进/后退语义保持；
- 旧深链接使用 replace 而非 push，避免 Back 循环；
- 不改变 `gui/dist` 手工文件；
- 不改变配置格式来完成纯视觉改版；
- 不触发可选 Lab 进入核心请求路径；
- 用户现有主题、语言和数据保持；
- 新导航只能重新组织入口，不能建立平行配置状态；
- 实施期间上游 `dev` 前进时，每个里程碑开始前重新核对相关 API 和文档。
- 托管能力以独立命令显式启用；升级不能把既有本地安装自动变为公网服务；
- 一期托管数据库仅支持单节点，进程锁失败时拒绝启动，不进行隐式多实例竞争。

## 18. 实施里程碑

### M0：基线与视觉契约

- 变基最新 `upstream/dev`；
- 保存改版前桌面/移动截图；
- 固化路由、API、i18n 和安全边界清单；
- 确认视觉 Token 和 Route Trace 数据契约。

### M1：Landing Page

- 重构 `Landing.astro` 和 `.lp` 样式；
- 完成真实内容、拓扑、快速开始和响应式；
- 保持现有语言和 SEO/JSON-LD；
- 完成 docs build 与截图。

### M2：设计系统与应用壳

- Token、页面 Header、导航分组、状态、表格、抽屉和移动端；
- 保留主题、语言、Stop/Restart 和 GitHub 用户同意边界；
- 完成 Hash 兼容测试。

### M3：资源管理

- 总览；
- 提供方和账户池；
- 模型目录、自定义模型、组合、路由、兼容性；
- API Key 一级入口。

### M4：观测与解释

- 用量；
- 请求历史 cursor 列表；
- 请求详情；
- Route Trace 和路由决策；
- 路由分析。

### M5：Agent、集成与系统

- 子代理；
- Codex Auth；
- 客户端集成；
- 启动、更新、调试、存储、清理、恢复和停止。

### M6：完成度与发布准备

- 全语言；
- 无障碍；
- 明暗主题；
- 全断点视觉回归；
- 文档同步；
- 测试、构建、隐私扫描；
- PR 描述与 GUI 截图。

### M7：托管安全基座

- SimpleCard 许可证、依赖、认证、授权、卡密、支付和部署审计完成，直接合并结论为 NO-GO；
- 建立 `hub` 显式启动入口、独立公开监听器、单节点数据库与独占锁；
- 完成 schema 迁移、一次性管理员 bootstrap、安全会话、Origin/CSRF 与限速；
- 证明普通 `ocx start` 的零导入、零数据库、零监听器、零定时器。

### M8：用户、密钥、账本与卡密

- 注册、登录、退出、会话列表、修改密码；
- 用户 API Key 一次显示、摘要存储、掩码列表和撤销；
- 整数额度、追加式账本、管理员调整；
- 卡密批次生成/导入/导出一次、停用、并发一次兑换；
- 用户门户与管理员页面完成响应式、无障碍和中英文首发文案，随后补齐现有语言。

### M9：中转准入与结算

- OpenAI Responses、Chat Completions、Anthropic Messages 与 Models 的流式透传；
- 用户 Key 认证、额度预留、上游 2xx 接受后固定结算、接受前释放和幂等；
- 客户端取消传播、超时、上游 401/429/5xx，以及接受后取消/崩溃的保守结算策略；
- 公开用户、内部准入与管理面隔离回归；
- 托管运维、安全和灾难恢复文档。

### M10：托管发布门

- 安全测试矩阵、严格 typecheck、全量测试、隐私扫描和高危依赖审计通过；
- 登录、充值、Key 创建、流式调用、余额不足、撤销和管理员流程完成真实浏览器验收；
- 威胁模型、备份恢复、密钥轮换、审计保留和单节点限制完成文档化；
- 不接支付通道，不声明多实例、高可用或商业上线，除非分别获得证据和批准。

## 19. 一期 Definition of Done

只有以下全部满足，才可以称为“一期全部完成”：

### 功能

- 本文 In Scope 页面均实现；
- 所有原有功能均有可达入口；
- 关键 CRUD、测试、同步、路由、清理和恢复流程可用；
- 路由轨迹来自真实请求历史，不是静态动画；
- 概念图中的非现有 API Key 能力已删除或明确禁用。
- 托管用户可完成注册/登录、卡密充值、创建用户 Key 和受额度约束的流式请求；
- 托管用户可读取自己的任务模型别名、公开路由、上游状态、扣费终态与非敏感终止原因；
- 管理员可安全创建/导入卡密批次、停用用户、调整额度并读取审计记录；
- 公开用户凭据无法访问 OpenCodex 管理面，普通本地模式未激活任何 hub 资源。

### 质量

- 空态、错误态、加载态、陈旧态和 N/A 均覆盖；
- 桌面和移动端完成；
- 明暗主题完成；
- 键盘和屏幕阅读器关键路径完成；
- 无新增隐私泄漏；
- 无未经审查的新依赖。
- 密钥和卡密只显示一次且数据库中无可恢复明文；
- 账本与卡密通过并发、重放、失败回滚和幂等测试；
- 无默认管理员、默认密码、占位 Secret 或忽略类型错误的发布路径。

### 验证

实施期至少运行：

```bash
cd gui
bun test tests
bun run lint
bun run lint:i18n
bun run build
```

Landing 变更运行：

```bash
cd docs-site
bun install --frozen-lockfile
bun run build
```

若修改 `src/` 或共享行为，再运行：

```bash
bun run typecheck
bun run test
bun run privacy:scan
bun run audit:high
```

托管安全变化还必须运行 focused security tests，至少覆盖会话固定、CSRF/Origin、登录限速、跨用户访问、一次性密钥显示、并发卡密兑换、重复结算、余额不足、日志脱敏、私有管理面隔离、流取消和本地模式零激活。

### 视觉验收

- 390×844；
- 768×1024；
- 1024×768；
- 1440×900；
- 1600×1000；
- 各一套浅色与深色关键页面截图；
- 减少动画模式检查；
- 无横向意外溢出、遮挡、焦点丢失和不可滚动抽屉。

### 文档与 PR

- `docs-site` 与实际行为一致；
- PR 目标为 `dev`；
- PR 模板完整；
- GUI PR 描述包含截图；
- 安全相关变化完成显式安全评审；
- 不声称未运行的测试或未验证的部署状态。

## 20. 风险与控制

| 风险 | 控制措施 |
| --- | --- |
| 视觉稿包含不存在能力 | 使用接口矩阵逐页验收，非现有能力移出一期 |
| 上游快速变化 | 开始实施和每个里程碑前重新核对 `upstream/dev` |
| 导航重组造成旧链接失效 | 保留现有 Hash 和兼容重定向测试 |
| 大规模 CSS 改造产生覆盖冲突 | 基于现有 Token，按页面逐步迁移并做截图回归 |
| 路由轨迹误导用户 | 区分配置预览、历史事实和无数据三种状态 |
| 成本被误读为账单 | 全局统一“列表价估算，不是账单” |
| API Key 页面暗示权限能力 | 只展示服务端真实支持的名称、用量、重命名和撤销 |
| 移动端复杂配置不可用 | 查看/诊断优先，复杂编辑使用全屏 sheet 或宽屏提示 |
| 新依赖扩大供应链风险 | 一期默认零新增运行时依赖 |
| 前端泄漏凭据或账号 | 类型验证、掩码、隐私测试和 privacy scan |
| 把公开用户面接入本地管理面 | 独立监听器、路由命名空间、凭据类型和隔离回归测试 |
| SimpleCard 依赖/实现风险进入主仓库 | 只作 MIT 领域参考；不引入 Next/Spring 运行时或依赖树 |
| 卡密、Key 或会话数据库泄漏 | HMAC 摘要存储、一次显示、密钥轮换和日志脱敏 |
| 并发兑换或重试导致重复加/扣额度 | SQLite 事务、唯一幂等键、追加式账本和并发测试 |
| 额度使用浮点产生漂移 | 全链路整数最小单位和版本化计价规则 |
| 公网误暴露 OpenCodex 管理端点 | 私有 loopback 目标、公开路由 allowlist、启动自检和负向测试 |
| 单节点 SQLite 被误当高可用 | 进程独占锁、文档化限制；多副本前必须实现 PostgreSQL 适配器 |
| 支付/钱包扩大资金与合规风险 | 一期不接支付、订单或钱包；后续需单独审批与安全评审 |

## 21. 已决策事项

1. 一期同时包含 Landing Page 和完整本地控制台；
2. 建设独立可选的用户登录、中转准入、整数额度和卡密功能；不建设钱包、支付、订单和订阅；
3. 不复制 New API AGPL 源码；
4. 设计语言以 hubapi 路由证据为核心；
5. 中文术语使用“提供方”；
6. API Key 提升为一级导航，但复用现有 Integrations 页面和接口；
7. 请求日志优先使用现有 cursor request-history 和 route-decision 接口；
8. 不新增字体、路由器、CSS 框架或遥测依赖；
9. Landing 不连接本地管理面；
10. 一期完成定义以本文第 19 节为准，而不是以静态效果图数量为准。
11. OpenCodex 是唯一提供方路由器；hub 只是公开准入、会话与结算边缘层；
12. 托管模式默认关闭，与本地管理面使用独立监听器和凭据；
13. SimpleCard 只作为 MIT 领域参考，不直接合并其 Next/Spring 应用；
14. 一期托管存储为单节点 SQLite，不宣称多实例或高可用；
15. 卡密和用户 Key 只显示一次、摘要存储；余额只能通过追加式整数账本变化。
