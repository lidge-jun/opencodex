---
title: "hubapi — One gateway for every AI model"
description: Universal provider proxy for OpenAI Codex & Claude Code — use any LLM with Codex CLI, App, SDK, and Claude Code.
template: splash
head:
  # Brand-first, no " | opencodex" suffix: Google matches the site name against
  # the home page title, so the wordmark leads and is not duplicated.
  - tag: title
    content: "hubapi — One gateway for every AI model"
  - tag: meta
    attrs:
      property: og:locale
      content: en_US
tableOfContents: false
---

import Landing from '../../components/Landing.astro';

<Landing />
